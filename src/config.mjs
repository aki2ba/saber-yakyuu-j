// ============================================================================
// 中央パラメータ（config）モジュール（自己レビュー M6 / F43）
//
// 較正の可変係数・NPB目標帯・試合数依存の媒介変数・リーグ定数の器を一箇所に集約する。
//   - 較正(1-11/2-11)は「このモジュールだけ」を調整対象にする（マジックナンバーを散らさない）
//   - §19が要求する「エンジンバージョン固定＝定数セットの凍結」の単一面を兼ねる
//   - 較正ランが共有状態を汚さないよう createConfig() で毎回ディープコピーを配る
// ============================================================================

export const CONFIG_VERSION = '0.10.0-farm'; // F2-5: ロスター拡大(70+育成)・出場登録29人環境への再較正

/** リーグ設定（フェーズA: 12球団×143試合・2リーグ制）。
 *  試合のDH有無は「ホーム球団の所属リーグ規則」に従う（旧 dh:'all' は廃止・S2で試合側に接続）。
 *  日程はS3日程v2: リーグ内5相手×25（ホーム13/12交互）＋交流戦6相手×3 → 143試合。 */
export const LEAGUE_DEFAULT = {
  numTeams: 12,
  gamesPerSeason: 143, // NPB準拠 = リーグ内125 + 交流戦18（S3日程v2）
  // 2リーグ制（名称は完全架空の造語）。dh=false のリーグは投手が打席に立つ（エンジン能力としては維持）。
  // 2026-07-25 ユーザー決定「投手が打席に立つのは廃止して全部DHにして」＝全リーグDH制へ統一。
  //   req_2②「現状のセパ両リーグ近似」からの意図的乖離（セ・リーグ系の投手打席という現実の非対称性を捨てる）。
  //   dh:false 経路自体（buildDepthChart/game.mjs）はテスト資産・将来のルール変更オプションとして削除しない。
  leagues: [
    { id: 'L1', name: '陽炎リーグ', dh: true }, // 旧セ・リーグ系（2026-07-25よりDH制へ）
    { id: 'L2', name: '蒼天リーグ', dh: true }, // パ・リーグ系（DH有り）
  ],
  inLeagueGamesPerOpp: 25, // リーグ内 同一相手との試合数（ホーム13/12を交互に）
  interLeagueGamesPerOpp: 3, // 交流戦 同一相手との試合数（3連戦を一方の本拠地で）
  rotationSize: 6, // 先発ローテ人数（中6日・NPB標準）
  rosterActive: 29, // 出場登録人数（F2-2: 支配下70人からシーズン開始時に球団AIが29人を選抜。NPB=出場登録29人相当）
  // 二軍リーグ（F2-2）: 同じ12球団のファームを2リーグ（完全架空名・一軍と同分割）で並走させる。
  //   一軍と同じ simulateGame・別の集計器(farmStats)。日程は一軍と同じ day カレンダーに載せる（同日消化）。
  //   null にすると二軍リーグを組まない（一軍結果は独立ストリームゆえ有無で byte 不変＝重い多年テストの節約用）。
  farm: {
    leagues: [
      { id: 'F1', name: '若草リーグ', dh: true }, // 一軍L1球団のファーム（二軍は両リーグDH制＝育成試合の簡略）
      { id: 'F2', name: '暁リーグ', dh: true }, // 一軍L2球団のファーム
    ],
    inLeagueGamesPerOpp: 22, // リーグ内 5相手×22 = 110試合（NPBファーム~110-130試合帯の下限側）
    interLeagueGamesPerOpp: 0, // 二軍の交流戦は無し（総試合 660 = 110×12/2）
    gamesPerSeason: 110,
  },
  // ポストシーズン規則（§S3-3）: CS1st=2戦先勝 → CSFinal=1位に1勝アド・4勝先取 → 日本シリーズ=4勝先取(2-3-2)
  postseason: {
    csFirstWins: 2, // CSファーストの必要勝数（2位本拠地）
    csFinalWins: 4, // CSファイナルの必要勝数（アドバンテージ込み・1位本拠地）
    csFinalAdv: 1, // リーグ1位のアドバンテージ勝数
    japanSeriesWins: 4, // 日本シリーズの必要勝数
  },
};

/**
 * 較正目標帯（フェーズA・12球団143試合2リーグ制 / phaseA_spec.md の表を全実装）。
 * 各値は [min,max]（片側条件はスカラー）。tools/calibrate.mjs の機械判定に使う（S4で結線）。
 */
export const CALIBRATION_TARGETS = {
  batting: {
    avg: [0.255, 0.262], // リーグ合算
    obp: [0.322, 0.332], // 全リーグDH制(2026-07-25): ほぼ自動アウトの投手打席(~3%のPA)が消えリーグOBP+0.003
    //   → セパ混合実測からパ・リーグ型（DH有）実測へ再アンカー（旧[0.32,0.328]・実測0.3284）
    slg: [0.39, 0.41],
    ops: [0.715, 0.735], // 下限を.715へ僅かに緩和（フェーズA）
    kPct: [0.18, 0.205], // 上限を.205へ（投手打撃の現実化で投手被Kが増え、リーグK%~.20はNPB水準として妥当）
    bbPct: [0.08, 0.087], // 全リーグDH制(2026-07-25): 四球をほぼ選ばない投手打者の消滅でBB%+0.002
    //   → パ・リーグ型実測へ再アンカー（旧[0.078,0.083]・実測0.084）
    // BABIP は独立ノブではなく AVG/K%/HR から算術的に決まる従属変数。
    // 「打球解決モデルが AVG/K%/HR と整合しているか」の冗長チェックとして門番に置く（正典§11.8）。
    // ⚠️NPB 2023-25 の実測は .288〜.292 だが、本シムの他の目標帯（AVG .255-.262 / OPS .715-.735 /
    //   HR 110-130 / 4.2点）は NPB 2015-19 の「飛ぶボール」時代の得点環境であり、BABIP も
    //   その環境と整合させる。低反発球時代へ寄せるなら得点環境一式の再ベースラインが必要（別課題）。
    babip: [0.298, 0.318],
    hrPerTeam: [110, 130], // per team / 143G ⚠️過剰HR＝最大の地雷＝第一の門番指標
    runsPerTeamPerGame: [3.9, 4.3],
    wobaLeague: [0.325, 0.335],
    // runDiffDhMinusNoDh は2026-07-25「全リーグDH制」化で削除。DH有無の対比リーグが存在しなくなり概念が消滅。
    //   （tools/calibrate.mjs は DH有/無 両方が存在するときだけこの手の差分を計算する一般化を実施済み）
  },
  pitching: {
    era: [3.6, 4.0], // リーグ合算。全リーグDH制(2026-07-25)で投手打席の自動アウトが消え得点環境+0.1
    //   → パ・リーグ型実測へ再アンカー（旧[3.5,3.9]・実測4.00。fip[3.6,4.0]と同帯に揃う）
    fip: [3.6, 4.0],
  },
  // タイトル級（リーグリーダーの分布の裾）
  leaders: {
    avg: [0.32, 0.355], // 打率王（帯を.355へ拡大）
    hr: [40, 55], // HR王
    rbi: [95, 140], // 打点王（ベンチ導入で低下想定）
    sb: [30, 65], // 盗塁王
    ipStarter: [150, 195], // 先発IPリーダー（中6日でもエースは深く）
    reliefG: [45, 65], // 登板数王（救援・連投制限下）
    sv: [30, 45], // SV王
    hld: [30, 45], // HLD王
  },
  // 采配の発現（犠打・敬遠・完投）
  tactics: {
    // shPerTeamNoDh は2026-07-25「全リーグDH制」化で削除（DH無リーグの投手バント込み犠打という区分が消滅）。
    shPerTeam: [30, 75], // 犠打/球団平均（全リーグDH制・NPBパ・リーグ実測準拠。旧 shPerTeamDh を両リーグ共通帯として存置）
    ibbPerTeam: [6, 40], // 敬遠/球団平均。全リーグDH制(2026-07-25): 「8番を歩かせて投手勝負」の動機が
    //   消滅し敬遠が減る（現実のパ・リーグもセより少ない）→ 下限10→6へ再アンカー（実測8.9）
    cgLeague: [5, 30], // 完投（リーグ計）
  },
  war: {
    totalLeague: [370, 430], // 12球団×143試合
    hitterShare: [0.53, 0.57], // 野手:投手 ≈ 55:45
    leaderHitter: [7, 9.5], // 野手WAR王
    leaderPitcher: [5.5, 8], // 投手WAR王
    // WAR下限（起用AIの機能証明・原則2「WAR-6の根絶」）を2本立てで判定する:
    //   floorCatastrophe: 全シード中の単一最悪値 > これ（系統的な起用崩壊＝WAR-4〜-6の根絶を保証）
    //   floorTypical:     各シーズンの最悪レギュラーWARの平均 > これ（典型的な最下位が破局的でない）
    // 単一minを典型閾値で判定すると、稀な貧ロスター（捕手難）の1選手で極値統計が落ちるため分離。
    floorCatastrophe: -4.0, // 単一最悪 > -4.0（絶対に -5/-6 を出さない）
    floorTypical: -2.5, // 各シーズン最悪の平均 > -2.5

  },
  // 起用・休養の発現
  usage: {
    qualifiedPerTeam: [5, 9], // 規定打席到達者/球団
    catcherStarterGames: [100, 135], // 正捕手の出場試合（143未満＝休養AIの発現）
  },
  // フェーズB B3c 追加系指標の健全性チェック帯（新指標の妥当域）。
  // ※これらは「追加集計」の健全性検証であり、上の既存30目標帯とは独立（既存30は不変を維持）。
  //   xwOBA≈wOBA・ΣRE24≈0・WPAゼロサム・平均LI=1.0 は構造上の恒等（モデル=シム / martingale）。
  //   LOB%/QS率/ARM/SD王 はNPB水準の妥当域（QS率は現行simが上振れ＝B1/全体較正で収束）。
  phaseB: {
    xwobaVsWoba: 0.003, // |リーグ xwOBA − wOBA| ≤ これ（打球モデル=シムの恒等）
    lobPct: [0.70, 0.75], // リーグ残塁率
    qsRate: [0.45, 0.60], // 先発QS率（NPB水準・B1一球化＋継投再較正で帯内に収束）
    armLeader: [5, 12], // 外野ARM上位（対リーグ平均run）
    ofAssistLeader: [5, 11], // 外野補殺リーダー（NPB.jp 2025実測: セ6 / パ9・正典§6.4）
    // --- 守備の門番（正典§11.8）。守備指標は長らく較正の対象外＝野放しだった。
    //   規定守備400イニング以上の野手UZR（捕手はUZRを持たない）。
    //   FanGraphs 目安「+15=ゴールドグラブ級 / −15=劣悪」。NPB(DELTA)実測も外崎+15.4(2022)・
    //   菊池+12.3(2014) と同水準で、143/162 の比例縮小を行う根拠は無い（正典§6.3）。
    uzrTop: [11, 20], // リーグ最高UZR
    uzrBottom: [-22, -11], // リーグ最低UZR
    uzrSd: [4, 7], // 規定守備者のUZR標準偏差（現実は約5）
    re24SumAbs: 1e-6, // |ΣRE24| ≤ これ（打者+走者+投手 = 0 恒等）
    wpaZeroSum: 1e-9, // WPAゼロサム最大誤差 ≤ これ（1試合 勝者±0.5）
    liAvg: [0.999, 1.001], // 打席加重平均LI = 1.0（正規化・aLI/pLI）
    sdLeader: [20, 50], // シャットダウン王（好救援の高レバレッジ成功）
    // --- B1-3 規律系（一球シムの副産物・リーグ集計の率）------------------------------
    pitchesPerPA: [3.7, 4.0], // 投球数/打席
    zonePct: [0.42, 0.48], // Zone%（明確にゾーン内の帯のみ・従来定義＝0.9.1でも不変）
    oSwingPct: [0.26, 0.34], // O-Swing%（ゾーン外スイング率・FanGraphs定義。0.9.1: ボーダー帯を
    //   半分ゾーン外として按分計上＝(際×0.5+明確)のスイング率。帯はNPB/MLB実測25-33%と整合し維持）
    zSwingPct: [0.60, 0.70], // Z-Swing%（ゾーン内スイング率）
    contactPct: [0.76, 0.82], // Contact%（スイングの接触率）。全リーグDH制(2026-07-25): 低コンタクトの
    //   投手打者(~3%のPA)が母集団から消え+0.7pt（野手の挙動は不変＝ノブで抑え込むと野手側が歪むため帯を再アンカー。
    //   旧[0.75,0.81]・実測0.818）
    swStrPct: [0.085, 0.12], // SwStr%（空振り/全投球）
    cswPct: [0.26, 0.31], // CSW%（見逃し+空振り / 全投球）
    fStrikePct: [0.58, 0.64], // F-Strike%（初球ストライク率）
    starterPitchesPerGame: [90, 110], // 先発投球数/試合（中6日と整合）
    wpPbPerTeam: [35, 70], // WP+PB/球団
    framingLeader: [6, 15], // フレーミングrun上位（捕手・対リーグ平均run）
  },
  // フェーズD2 パークファクター（§11.2）の健全性チェック帯（既存50とは独立の追加系）。
  //   得点環境は既存50が据え置きを保証（PF対称・ゼロサム）。ここは PF が「球団ごとに散る」ことと
  //   「PF補正後の wRC+ がリーグ100中心（＝補正がゼロサム）」を検証する。
  phaseD: {
    // PF散らばり（12球団の run PF の最大−最小）。⚠️1年の run PF は試合数有限の統計ノイズが支配的で、
    //   球場個性ゼロ（中立単一park）でも max−min≈0.29 の床がある。球場個性を載せると≈0.35へ増える
    //   （＝上位/下位が実効±10-17%に散る）。よって帯は「ノイズ床(0.29)＋球場signal」を含む水準に置く。
    pfRunsSpread: [0.24, 0.48], // max(pfRuns)−min(pfRuns)（ノイズ床0.29＋球場個性）
    pfRunsMean: [0.99, 1.01], // リーグ平均 pfRuns（正規化により≈1＝ゼロサム＝得点環境据え置きの担保）
    parkWrcPlusCenter: [98.0, 102.0], // PA加重リーグ park補正 wRC+（補正がゼロサム＝100中心）
  },
};

/**
 * B3a 追加系指標（一球データを要さない集計指標・§B3）の定義定数。
 * Barrel/HardHit/SweetSpot のEV/LA帯・方向境界・SIERA(FG公開式)/kwERA の係数を集約する。
 * ※これらは物理/定義に基づく固定値（較正ノブではない）だが、規約に従い config.tuning に集約し、
 *   metrics.mjs 側は cfg 未指定時にこの既定値へフォールバックする。
 */
export const METRICS_CONST = {
  hardHitKmh: 152, // HardHit% 閾値（EV≥152km/h ≈ 95mph）
  barrelMinKmh: 157.7, // Barrel下限EV（≈98mph）
  barrelBaseLo: 26, // 98mph時のBarrel帯 LA下限
  barrelBaseHi: 30, // 98mph時のBarrel帯 LA上限
  barrelLoSlope: 1.0, // mph超過1あたりLA下限を下げる度数（116mphで8°）
  barrelHiSlope: 1.11, // mph超過1あたりLA上限を上げる度数（116mphで50°）
  barrelMinLA: 8, // Barrel帯の最小LA
  barrelMaxLA: 50, // Barrel帯の最大LA
  sweetSpotLoLA: 8, // Sweet-Spot% の下限LA
  sweetSpotHiLA: 32, // Sweet-Spot% の上限LA
  centAbsSpray: 15, // |spray|≤これ=中堅方向(Cent)。外は引っ張り/流し
  // SIERA（FanGraphs公開式・Swartz）: netGB=(GB−FB−PU)/PA, SO/PA, BB/PA。
  // netGB²項は符号保存（GB>FB+PU で負寄与＝好投、逆で正寄与）＝ cNet2×netGB×|netGB|。
  siera: { c0: 6.145, cSO: -16.986, cBB: 11.434, cNet: -1.858, cSO2: 7.653, cNet2: -6.664, cSOnet: 10.13, cBBnet: -5.195 },
  // kwERA = 5.40 − 12×(K% − BB%)
  kwERA: { c0: 5.4, k: 12 },
};

/**
 * B2 文脈指標（RE24/WPA/LI・§B2）の定義/導出定数。
 * 状態キー（イニング/表裏/点差クリップ/塁アウト）の粒度と、勝率表(WE)の階層平滑化強度、
 * シャットダウン/メルトダウンの WPA 閾値を集約する。これらは較正ラン相当の大標本から
 * RE行列・WE表・LI表を導出する2パス（season.mjs で結線）で参照される。
 */
export const CONTEXT_CONST = {
  innMax: 9, // イニングのクリップ上限（延長は9回バケットへ集約）
  scoreDiffClip: 8, // 点差クリップ ±8（打者側視点 = 攻撃側得点−守備側得点）
  sdThreshold: 0.06, // SD(≥+0.06)/MD(≤−0.06) の1登板WPA閾値（FG方式）
  // WE 勝率表の階層平滑化: fine(24状態)←coarse(イニング/表裏/点差)←diff周辺←0.5
  weSmoothK: 40, // fine セル← coarse事前分布の平滑化擬似標本
  weCoarseK: 40, // coarse セル← diff周辺分布の平滑化擬似標本
  weDiffK: 20, // diff周辺← 0.5 への軽い平滑化擬似標本
};

/**
 * エンジン調整ノブ（初期値・較正で動かす）。§18主要定数＋新EV/LAエンジン固有ノブ。
 * 新エンジンでは BABIP/HR は打球格子から創発するため、旧「結果先決め」定数と1:1でない（F44）。
 */
export const TUNING_DEFAULT = {
  // B3a 追加系指標（率/期待値）の定義定数（§B3・上の METRICS_CONST を集約）。
  metrics: METRICS_CONST,
  // B2 文脈指標（RE24/WPA/LI）の導出/定義定数（§B2・上の CONTEXT_CONST を集約）。
  context: CONTEXT_CONST,

  hrScale: 0.9655, // 本塁打産出スケール（門番: hrPerTeam/HR王）。⚠️HRは閾値のため感度大。
  //   全リーグDH制較正(2026-07-25): smaxPerRating 0.018→0.017（守備上位層の圧縮）で被安打微増・
  //   HR/team 130.2と門番帯上限(130)を微超過→0.9695→0.9655（帯は緩めずノブで再収束）。
  //   守備モデル刷新でPA数がわずかに動き HR王 が帯を割ったため微増（0.966→0.9695・HR/team は帯中央を維持）
  //   F2-5: 出場登録29人選抜の上澄み効果でHR/teamが141へ膨張→0.985から引下げて[110,130]帯へ再収束
  babipBase: 0.3, // インプレー打球の安打基準
  fieldingCoef: 0.0009, // 守備係数（§18）
  // WAR代替水準（§9・§18の初期値。143試合/NPBへ較正対象）
  // 野手の代替水準（正典 sabermetrics_glossary.md §7.1 / §10.5）。
  //   FanGraphs: 代替勝利の総量 = 570 × (Games/2430) を固定し、打席比で按分する。
  //   ＝ 1チーム1試合あたり 570/(30×162) = 0.1173 wins。このシムは較正で値を決める。
  //   代替勝利を wins で固定することで、時代トレンド（得点環境の揺れ）で総WARが動かなくなる。
  replHitterWinsPerTeamGame: 0.142, // ※WAR較正（総WAR・野手WAR比の門番）
  //   全リーグDH制(2026-07-25): L1にもDHスロットが立ち、負のDH位置調整がリーグ全体へ倍加＋
  //   投手打席の消滅でリーグ平均打撃が上がり野手wRAAが相対圧縮 → 野手WAR比0.510と帯(0.53)を割った。
  //   FanGraphs流「代替水準は環境が変われば再較正する値」に従い0.129→0.142（野手WAR比≈0.535へ・
  //   総WAR394→約416で帯[370,430]内を維持）。
  //   R2再較正: 0.134→0.1355。年齢構造の導入で一軍29人の選抜が「成熟した上位層」に絞られ、
  //   リーグの能力分散が広がって総WARが帯(430)を +2 超過したぶんを代替水準側で吸収する
  //   （代替水準は本来「較正で決める値」＝能力分布が変われば再較正するのが筋。上の注記参照）。
  replBatterPer600: 19.1, // 【フォールバック専用】lc が無いときだけ使う旧式 (PA/600)×これ [run]
  // 投手の役割別代替水準（S3・FanGraphs方式 B-5。旧 replFipMult=lgFIP×1.25 を廃止）:
  // pitcherWAR = (lgFIP−FIP)/9×IP/RPW + (IP/9)×replPer9。replPer9 は GS/G で先発/救援を按分。
  replStarterPer9: 0.166, // 先発の代替水準（wins/9IP）※投手WAR王5.5-8の門番（総WAR/野手比とのトレードオフの均衡点）
  //   F2-5: 29人選抜でリーグ平均投手が上澄み化しエースの相対FIP優位が圧縮（WAR王5.3へ低下）→0.153から引上げ
  replRelieverPer9: 0.014, // 救援の代替水準（wins/9IP）※レビュー#2: 0.012はブルペン総WARを負に沈める→約0へ引上げ。
  //   F2-5: replStarter引上げの総WAR超過(430上限)を救援側で相殺（0.020→0.014・ブルペン総WARは非負を維持）
  // FanGraphs方式（救援は先発より低い代替水準だが極端でない）へ寄せ、ブルペン総WARを僅かに正へ。

  // 打席規律層（1-1）: log5/オッズ比で K/BB/HBP/インプレー を分岐する較正ノブ。
  // League は打席1回あたりの基準確率、Slope は能力(20-80)→logit の感度。
  pa: {
    kLeague: 0.191, // リーグK率（NPB ~19-20%）
    bbLeague: 0.0795, // リーグBB率 ※較正済み
    hbpLeague: 0.009, // リーグHBP率
    kSlope: 0.22, // K感度 ※較正済み（分布の裾M4を圧縮）
    bbSlope: 0.2, // BB感度 ※較正済み
    hbpSlope: 0.1, // HBP感度（投手制球の荒れ）
    kContactW: 0.65, // 打者K傾向へのコンタクト寄与 ※S5較正（打者側K裾の圧縮・投手側は不変）
    kEyeW: 0.3, // 打者K傾向への選球眼寄与
    kVeloPerKmh: 0.85, // 投手奪三振への球速寄与 ※エース級K裾＝投手WAR王の門番
    // 球種格子 段階1（2-1）: 1打席ごとに投手の球種を1つ選び、その whiff で解決。
    fastballWeight: 2.0, // 球種選択で速球系を重く（残りは1.0）
    whiffAptW: 0.24, // 打者の対該当クラス適性が高いほどKしにくい
  },

  // ==========================================================================
  // 一球ごとシミュレーション（B1・§B1）: (balls,strikes) カウント状態機械のノブ。
  // plateAppearance.mjs の runPlateAppearance が消費する。K/BB/HBP は 3ストライク/4ボール/
  // 死球として創発する（旧 pa.* の log5 一発抽選は廃止し legacy 化）。全ノブをここへ集約。
  // 較正の第一目標帯（§B1-3）: 投球数/打席3.7-4.0・Zone%42-48・O-Swing26-34/Z-Swing60-70・
  //   Contact75-81・SwStr8.5-12・CSW26-31・F-Strike58-64。
  // ==========================================================================
  pitch: {
    // --- 投球ロケーション（ゾーン内/ボーダー/明確ボールの3帯） ---
    zoneBase: 0.4085, // 基準ゾーン内率（control50・even count）※Zone%の主ノブ（B1較正: 0.42→0.432。
    //   F2-5: 29人選抜で制球上位が残りリーグBB%が6.6%へ沈没→0.432から引下げてBB%[7.8,8.3]/K%/F-Strikeを回復。
    //   選抜後の実効Zone%は[42,48]帯内=44-45%に留まる。
    //   選手アイデンティティ較正（2026-07-20）: 0.399→0.4015。名前キー生成へのシード世界引き直しで
    //   BB%が帯上限(0.083)を超過→F2-5と逆方向の微調整でBB%/K%を同時に帯へ戻す。
    //   苗字プール拡張（326→713）の再引き直しで四球寄りに再度振れ（BB%/OBP/ERAが帯上限超過）
    //   → 0.4035。名前拡張（96→368・最終プール）の引き直しで再度BB%0.084 → 0.4055で最終収束。
    //   ※アイデンティティ経路の生成は旧経路比で真の平均が微打者寄り（~0.02σ）＝3連続で同方向に
    //     振れた機序。プールはこれで確定なので以後の引き直しは起きない）
    zoneControlW: 0.005, // 制球(50中心)→ゾーン率（制球でストライク先行）
    zoneAheadW: -0.10, // 追い込み(0-2,1-2)でゾーン率減（ボールで釣る）
    zoneBehindW: 0.15, // ビハインド(3-0,3-1,2-0)でゾーン率増（置きにいく）
    zoneEvenBehindW: 0.04, // balls>strikes の軽いビハインドでゾーン率増
    // 帯配分の現実化（0.9.1-pitchband・ユーザーレビュー「クソボールばかり」対応）:
    //   Statcast帯換算では際(shadow)が全投球の~40%・明確ボール(chase/waste)は~30%。旧0.34は
    //   際18.8%/明確36.6%と歪んでいた（際が少なく大外れが多い）→ 0.56で際~31%/明確~24%へ。
    borderShare: 0.56, // 非ゾーンのうちボーダー帯（フレーミングが効く縁）の割合。残りは明確ボール
    // --- 打者スイング判断 ---
    zSwingBase: 0.62, // ゾーン内スイング率（Z-Swing%の主ノブ・B1較正: 0.66→0.62で早見せ→投球数/CSW↑）
    bSwingBase: 0.46, // ボーダー帯スイング率（0.9.1: 据置。際の球数自体が~1.6倍になるため base 据置でも
    //   際スイングの絶対量は増え、chase減と合わせて全体swing量はほぼ不変）
    oSwingBase: 0.10, // ボール球スイング率（chase・0.9.1: 0.235→0.10。明確ボールへの手出し~30%は現実(~18%)比1.7倍
    //   → 2ストライク保護スイング/同利き補正込みの実効で~15-18%へ現実化。BB%はborderCsBase/whiff系で再収束）
    swingEyeW: 0.004, // eye(50中心)→ボーダー/ボール球スイング率減（見極め。0.9.1: 0.006→0.004。
    //   chase基準の半減で eye 差の相対効果が増幅され、高eye打者のchaseが下限クランプに張り付いて
    //   規律の質差が過大に→セパ得点差(DH規則単位)が帯上限超え＋野手WAR王9.5超え。
    //   傾き圧縮で両者を帯へ戻す（0.005では野手WAR王9.6が残存→0.004で~8.9）
    swingZoneEyeW: 0.0015, // eye→ゾーンスイング率減（わずか。巧打者は good pitch を待てる）
    twoStrikeSwingW: 0.20, // 2ストライクの保護スイング増（全帯・B1較正: 0.11→0.20。当てにいく→ファウル粘り）
    threeOhTakeW: 0.42, // 3-0での自重（スイング率減）
    // --- スイング時の空振り ---
    whiffZoneBase: 0.1845, // ゾーン内スイングの空振り基準（B1較正: 0.095→0.175。0.9.1: →0.185。
    //   帯再配分で空振りの多いchase swingが減った分をゾーン内で補い Contact%[75,81] を維持。
    //   F2-5: whiffPitchW引上げ(0.006→0.008)が選抜後の上澄みリーグでK%を押し上げるため基準3本を引下げて相殺。
    //   苗字プール拡張較正（2026-07-20）: zoneBase引上げでContact%が0.811と帯上限を微超過→+0.002で相殺。
    //   采配妥当性ゲート較正（2026-07-23）: バント2ゲートの引き直しでContact%が0.810と帯上端を微超過→+0.0015で相殺
    //   全リーグDH制較正（2026-07-25）: smaxPerRating 1ノッチの被安打微増で攻撃全体が過熱
    //   （AVG 0.262帯上端・R/G 4.36・ERA 4.07）→ 0.1825→0.1845（+0.002の前例幅・K%は0.196で余裕）で相殺）
    whiffBorderBase: 0.303, // ボーダー帯スイングの空振り基準（B1較正: 0.16→0.27。0.9.1: →0.32。F2-5: →0.303・同上）
    whiffOBase: 0.417, // ボール球スイング（chase）の空振り基準（B1較正: 0.26→0.38。0.9.1: →0.44。F2-5: →0.417・同上）
    whiffPitchW: 0.008, // 球種whiff(50中心)→空振り増（F2-5: 0.006→0.008。29人選抜でエースの相対K優位が
    //   圧縮され投手WAR王が帯割れ→投手間スプレッドを拡大しエースの裾を回復。リーグK%は基準3本の引下げで相殺）
    whiffContactW: 0.006, // 打者contact(50中心)→空振り減
    whiffAptW: 0.004, // 対該当クラス適性(50中心)→空振り減
    whiffTwoStrikeW: 0.256, // 2ストライクの空振り率減（当てにいく短縮スイング。ppa↑・K抑制・B1較正: 0→0.18。
    //   0.9.1: →0.215。F2-5: →0.235（選抜後のK%を[18,20.5]上限内に収める最終相殺）
    //   選手アイデンティティ較正（2026-07-20）: 0.248→0.256。シード世界引き直しのK%上限超過(0.206)を
    //   BB%へ波及しないこのノブで相殺（zoneBase+0.0025のK%押上げぶんを含む）
    // --- 接触時: ファウル vs インプレー ---
    foulBase: 0.47, // 接触のうちファウルになる基準
    foulTwoStrikeW: 0.18, // 2ストライクでファウル率増（粘り＝カット・B1較正: 0.08→0.18で投球数/PA↑）
    // --- 見逃し時: ボーダー帯の捕手フレーミング判定 ---
    borderCsBase: 0.168, // ボーダー帯見逃しのストライク獲得基準（framing50=リーグ中立時。0.9.1: 0.24→0.183。
    //   際の球数~1.6倍で見逃しストライクが増えBB%が沈むため、獲得率を下げて BB%[7.8,8.3] を回復。
    //   F2-5: →0.175（zoneBase引下げと合わせたBB/K再配分の微調整）
    //   選手アイデンティティ較正（2026-07-20）: 0.170を試したが K%/フレーミング上位を押し上げ
    //   BB%はほぼ不動＝このノブはBB微調整に不適（見逃し三振とフレーミング計数へ直結）。0.168のまま。
    frameSlopePerPt: 0.0024, // framing(50中心)→ボーダーCS率±（一球単位の創発。0.9.1: 0.0040→0.0024。
    //   際が18.8%→~31%へ増えフレーミング機会が~1.6倍になったため、上位run[6,15]を保つよう傾きで相殺）
    runPerCall: 0.125, // 1コール(vs中立)→run（framingRuns。per-inning近似の置換・§7.3）
    // --- HBP（内角外れの低確率イベント。現行率~0.9%/PAを維持） ---
    hbpPerClearBall: 0.0113, // 明確ボール1球あたりのHBP率（0.9.1: 0.007→0.0104。明確ボール36.6%→~24.7%の
    //   帯再配分でHBP機会が0.675倍になったため率で相殺＝HBP/PA~0.9%を維持。F2-5: →0.0113（OBP下限の回復））
    hbpControlW: 0.006, // 制球が低い(50-control)ほどHBP増
    // --- 暴投/捕逸（ワンバウンド球×捕手blocking） ---
    dirtBaseBreaking: 0.16, // 明確ボール(変化球)がワンバウンドになる率（0.9.1: 0.11→0.16。帯再配分で
    //   明確ボールが0.675倍→WP+PB/球団[35,70]を維持するよう率で相殺）
    blockSlopePerPt: 0.005, // blocking(50中心)→ワンバウンド抜け回避
    wildBase: 0.52, // ワンバウンド×走者ありで球が抜ける基準（blockで減）
    wpShare: 0.6, // 抜けたうち暴投(投手起因→wp)の割合。残りは捕逸(捕手起因→pb)
    // --- プラトーン/TTO の一球パラメータ再配置（旧 pa.platoon/tto の log5 相当を一球へ） ---
    platoonWhiffSame: 0.03, // 同利きで空振り率増（K↑）※D1-2: baseline維持（league K%を帯内に保つため据置）
    platoonOSwingSame: 0.05, // 同利きでchase増（BB↓）※D1-2 0.03→0.05（同利きBB抑制を強化）
    ttoWhiff: 0.010, // 巡目(tto)ごとに空振り率減（打者が慣れる＝K↓・EV↑は bb.tto側）
    // --- 球種選択のカウント依存重み（selectPitchByCount） ---
    fastballWeight: 2.0, // even時の速球系重み（残り球種=1.0）
    putawayWhiffBias: 0.9, // 2ストライク時、高whiff球種(whiff-50)に比例して重み増（決め球）
    behindFastballBias: 1.6, // ビハインド(3ball/2-0)で速球系重みを追加（制球しやすい球）
  },

  // 左右プラトーン（S1・M7解消）: 同利き手（実効打席サイド==投手の利き腕）へのペナルティ。
  // スイッチ(S)は常に投手と逆打席＝有利側に立つ（同利きにならない）。
  // 効果量の初期値は同利きで wOBA −.020〜.030 相当（S5較正で調整）。
  platoon: {
    // D1-2: 同利きwOBAを −.012→−.023 へ引上げ（目標帯 −.020〜.025・レビュー#4残差の解消）。
    //   ⚠️一球シム(runPlateAppearance)で実際に効く同利きノブは【pitch.platoonWhiffSame(K側)・
    //     pitch.platoonOSwingSame(chase/BB側)・この evKmhSame(EV→wOBA側)】の3つ。
    //     kLogitSame/bbLogitSame は旧 log5 一発抽選(paProbabilities)専用＝legacy（現行シムは不使用・
    //     単体テストのみ）。よって wOBA差の主ノブは evKmhSame とし、K%を膨らませないよう whiff は
    //     baseline維持・chase(oSwing)のみ増。EV減で落ちたリーグ得点環境は bb.evBase(139→139.55)で
    //     全体を再センタリング（同利き差=相対値は不変・§D1-2）。
    kLogitSame: 0.18, // 【legacy】同利きで K の logit 増（paProbabilities専用・現行シム不使用）※D1-2で意図に合わせ0.10→0.18
    bbLogitSame: -0.14, // 【legacy】同利きで BB の logit 減（同上）※D1-2 -0.08→-0.14
    evKmhSame: -2.5, // 同利きで打球EVの中心を下げる (km/h)※D1-2 -1.2→-2.5（一球シムで効く wOBA差の主ノブ）
  },

  // 犠打（S2 maybeBunt が消費。§S2-4）: 試行判断・結果テーブル。2ストライク概念はフェーズB。
  // §tactics_re（2026-07-23）: attemptProb は RE(得点期待値)比較の decisionScore で駆動する
  // （buntDecisionScore・manager.mjs）。attemptBase は decisionScore=0 相当時の基準率として残す。
  bunt: {
    successProb: 0.78, // 成功（走者進塁・打者アウト・sh++・ABなし）
    failProb: 0.12, // 失敗（先頭走者アウト）。残り＝内野安打
    hitProb: 0.1, // 内野安打化
    maxScoreDiff: 2, // 接戦判定（±この点差以内で試行）
    attemptBase: 0.17, // 非強打者×バント局面の基本試行率 ※S2予備調整（野手SHがセパ差を埋没させない水準へ）
    //   守備モデル刷新(Distance-Time)＋ARM実イベント化でバント局面の期待値が変わり犠打が目減りしたため再較正(0.145→0.17)
    //   F2-5: 0.16→0.145（DH無リーグの攻撃力を僅かに回復しセパ得点差を帯上限0.45から離す。SH帯は維持）
    tendW: 0.5, // 監督buntTend(50中心)の感度（logit増分/10pt）
    pitcherAttempt: 0.8, // 投手打席はほぼ必ずバント（F2-5: 0.9→0.8。セパ得点差の再収束＝投手にも僅かに打たせる）
    //   2026-07-25: 全リーグDH制で投手が打席に立たなくなり休眠（このノブは参照されなくなる）。
    //   エンジンのDH無し能力（buildDepthChart {dh:false}・game.mjsのdh:false経路）とともに維持。
    pitcherMaxScoreDiff: 6, // 投手はこの点差以内でのみバント（大差では打たせる。野手より広い）
    weakBatterWoba: 0.3, // 「非強打者」の目安（観測wOBAがこれ未満）※S2予備調整
    pitches: 2.5, // バント打席の投球数近似（S2）
    // --- §tactics_re: RE(得点期待値)比較の重み ---
    reScale: 4.4, // ΔRE(runs)→logit変換スケール ※tactics_re較正
    npbBias: 0.75, // NPBの監督はRE最適より多く送る（セ寄りの犠打文化）へ寄せる加算logit ※tactics_re較正
    nextBatterW: 0.55, // 次打者観測wOBAの寄与重み（バントが次打者へ渡す得点圏の価値の按分）
  },

  // 敬遠（S2 maybeIBB が消費。§S2-5）: 一塁空き×2死or一死×終盤接戦×強打者（or次打者が投手）。
  ibb: {
    base: 0.7, // 条件成立時の基本敬遠率 ※S5較正（敬遠10-40/球団へ）
    tendW: 0.5, // 監督ibbTend(50中心)の感度
    minInning: 7, // 終盤のみ
    maxScoreDiff: 2, // 接戦のみ
    strongBatterWoba: 0.36, // 「強打者」の目安（観測wOBA上位）
    pitches: 4, // 敬遠の投球数（ボール4球）
  },

  // §tactics_re（2026-07-23・ユーザー指示「得点期待値を考えて比較。プログラムでできるはず」）:
  // 「監督の頭の中のRE表」＝ 24状態(塁8×アウト3)の得点期待値。バント/敬遠のRE比較判断
  // （manager.mjs buntAttemptProb/buntDecisionScore・ibbProb）と盗塁の損益分岐ゲート
  // （stealLogitAdjust）が参照する。実際の監督も現在シーズンの途中経過から毎回RE表を
  // 再計算はしない＝固定較正値として持たせる（鉄則2「config集約」＝この表もtuningの一部）。
  //
  // 【導出手順（再導出可能性の担保）】この表自体がバント/敬遠/盗塁の判断に使われる＝RE表を
  // 変えると採る戦術が変わりゲーム内乱数消費列も変わる自己参照構造のため、単一seedでは希少状態
  // （0死3塁単独・0死満塁など年間数百例の状態）の推定が振れる。8seed平均で実測→config反映→
  // 再実測を数周（今回3周）繰り返して収束させた（残差 |diff| は概ね<0.05・最大でも<0.15）:
  //   const SEEDS = [2026,2027,2028,2029,2030,2031,2032,2033];
  //   const cfg = createConfig({}); // その時点のconfig（reTableは前回値）を使って参照走行
  //   const sums = new Array(24).fill(0);
  //   for (const seed of SEEDS) {
  //     const lg = generateLeague(seed, cfg);
  //     const res = simulateSeason(lg, cfg, { season: 2026, seed, postseason: false, context: true });
  //     for (let i = 0; i < 24; i++) sums[i] += res.contextTables.re[i];
  //   }
  //   const avg = sums.map((x) => Math.round((x / SEEDS.length) * 1000) / 1000); // → 下記reTableへ貼る
  //   // これを新しいreTableとしてconfigへ反映し、収束するまで再実行（今回3周）。
  // 【導出時seed】2026-2033（上記8シード平均・季節=2026固定）。
  // 【実測値（2026-07-23 導出・3周収束後）】
  //   outs=0: [0.461, 0.816, 1.004, 1.390, 1.225, 1.602, 1.827, 2.217]
  //   outs=1: [0.249, 0.480, 0.647, 0.856, 0.813, 1.051, 1.262, 1.486]
  //   outs=2: [0.096, 0.211, 0.313, 0.439, 0.384, 0.482, 0.597, 0.789]
  //   （breakeven盗塁成功率が0.71〜0.75前後と算出され、既存 steal.successBase=0.72 の
  //     「損益分岐~70%近辺」という設計コメントと自然に一致＝この参照RE表がシムの内部整合性と
  //     噛み合っている根拠。test/tactics_re.test.mjs も同じ8seed平均で近似一致±0.15を検証）。
  tactics: {
    reTable: [
      0.461, 0.816, 1.004, 1.390, 1.225, 1.602, 1.827, 2.217,
      0.249, 0.480, 0.647, 0.856, 0.813, 1.051, 1.262, 1.486,
      0.096, 0.211, 0.313, 0.439, 0.384, 0.482, 0.597, 0.789,
    ],
    // IBBによるRE増（=守備側のRE損）がこれを超える局面は禁止。ibbProbの成立条件（bases[0]空き
    // ×得点圏あり）では、この engine の IBB は定義上つねに満塁化を伴う（1塁を歩かせて埋める）ため、
    // 「満塁化=つねに発生する通常ケース」であって「例外的にRE損が大きすぎる異常値」ではない。
    // よって閾値は実測ΔRE（一死: 二塁単独0.209/二三塁0.224/三塁単独0.238・二死: 0.126/0.192/0.098）
    // の最大値より十分高く取り、通常運用では作動しない「軽いサニティゲート」（既存の較正済み
    // 敬遠帯[10,40]/球団を壊さない・保険専用）として持たせる。RE表が将来大きく動いた場合や、
    // ibbProbのゲート条件が緩和された場合の異常値だけを弾く安全弁。
    ibbMaxReLoss: 0.30,
  },

  // 交代（S2 代打/代走/守備固め。§S2-3）
  sub: {
    phInning: 7, // 野手への代打は7回以降
    phPitcherInning: 5, // 投手への代打は5回以降（F2-5: 6→5。29人登録でベンチが厚くなった分DH無リーグの
    //   代打起用を現実寄りに前倒し＝セパ得点差[0.1,0.45]の再収束の主ノブ）
    //   2026-07-25: 全リーグDH制で投手が打席に立たなくなり休眠（このノブは参照されなくなる）。
    //   エンジンのDH無し能力とともに維持。
    phMaxBehind: 3, // ビハインドこの点差以内（or接戦）の得点機で発動
    phGainMin: 5, // 代打起用に要する実効打力差（プラトーン込みhitScore相当）
    phPlatoonW: 10, // プラトーン補正のhitScore換算（同利きの候補はこれだけ減点）
    prInning: 8, // 代走は8回以降
    prMaxScoreDiff: 2, // 2点差以内
    prSpeedGainMin: 10, // ベンチ最速との走力差がこれ以上
    defInning: 8, // 守備固めは8回以降
    defLeadMin: 1, // リード1〜3で発動
    defLeadMax: 3,
    defGainMin: 8, // 守備固めに要する守備スコア差（習熟+素材。優位時のみ交代）
  },

  // 監督の観測評価（S2 manager.mjs）: インゲーム判断用の観測wOBA近似。
  // 三層構造の原則: 采配は観測statlineのみを見る（trueAbility は編成時評価に限る）。
  mgr: {
    wobaScale: 1.24, // 生の得点価値/PA → wOBA 換算の簡易スケール（リーグ定数導出前の近似）
    wobaPrior: 0.323, // 少打席の回帰先（リーグ平均wOBA相当）
    wobaPriorPA: 60, // 回帰の事前打席数（PAが少ないほど平均へ寄せる）
  },

  // 継投（S2 継投v2。§S2-7）: 役割ベース（closer/setup8/setup7/middle/long）＋降板判定の閾値。
  pen: {
    saveLeadMax: 3, // セーブ機会のリード上限（1〜3点差）
    leverageMinInning: 7, // 勝ちパターン継投の開始回（7回=setup7）
    starterPitchBase: 70, // 先発の球数上限の基礎（+ stamina×starterPitchStamW）
    starterPitchStamW: 0.68, // スタミナ→球数上限の感度 ※S5較正（高スタミナ先発の深投~110球）
    //   F2-5: 0.64→0.68（エースの深投＝投手WAR王の回復。QS率は代打前倒し(sub.phPitcherInning)で帯内に収まる）
    quickHookW: 0.44, // 監督quickHook(50中心)→球数上限の減分（早い継投ほど上限低）
    starterMaxRuns: 6, // 先発の失点即降板ライン
    tiredOuts: 18, // 6回以降（アウト数）で
    tiredRuns: 4, // 4失点なら降板
    starterStayRuns: 1, // 7-8回のセーブ機会でも失点これ以下の先発は続投 ※S5較正（8回はセットアッパーへ＝HLD王の門番）
    cgMinOuts: 21, // 9回続投（完投・完封狙い）に必要な先発アウト数（かつ無失点）
    cgMaxPitches: 80, // 9回続投を許す球数上限（完投の門番・S5較正。リード有無に依らず適用）
    relieverMaxOuts: 3, // 勝ちパターン役割（closer/setup8/setup7）は基本1イニング
    middleMaxOuts: 6, // middle（非役割）は複数イニング可（登板数王の圧縮・S5較正）
    relieverMaxRuns: 3, // 救援の失点降板ライン
    longOuts: 8, // 敗戦処理ロングは2-3回を投げる
    bigBehind: 5, // これ以上のビハインド=敗戦処理（longへ）
    // D4 レバレッジ駆動継投（§8.3の完成）: 接戦度(LI代理=回×点差×走者/アウト)で最良救援を高LI場面へ。
    //   代理LIは状態の純関数＝pass1/pass2で不変（決定論・WPA telescoping を壊さない）。
    levInningPivot: 7, // レバレッジ回重みの起点（7回=1.0）
    levInningSlope: 0.3, // 1回ごとの重み増（8回1.3 / 9回1.6 / 延長…）
    levInningCap: 12, // 回重みの頭打ち（延長の暴走防止）
    levCloseScale: 2.5, // 点差の減衰スケール（同点=1.0・離れるほど指数減衰）
    levRunnerW: 0.35, // 走者1人あたりの重み
    levRispW: 0.25, // 得点圏（2B/3B）加点
    levOutW: 0.15, // アウト1つあたりの減衰
    highLevThreshold: 1.6, // 高レバレッジ判定閾値（この上で最良セットアッパーを投入・S5較正で調整）
    // 破綻救援ガード（多年運用・原則2「WAR-6の根絶」の投手版・捕手の壊滅ガードと同型）。
    //   前年の観測失点率(RA9=r*27/outs)が破綻水準の"前歴"があり、かつ当年も観測で不振を確認できる
    //   救援を、当日ブルペン可用リストから確率的に外す（連投蓄積を止め破綻救援のIP膨張＝投手WARの
    //   単調悪化を防ぐ）。三層構造: 真値は見ず前年＋当年の観測RA9のみで判定。
    //   ★1年目不変の保証: 判定には"前年の観測ライン"が必須で、1年目は前年が存在しない（priorPitch 空）
    //     ため全員 前歴なし＝ガードは一切作動しない（較正53指標・SV/HLD/登板数王が byte 不変）。
    //     ゆえに以下の閾値は1年目較正に無影響で、多年の健全性（6seed×60年で最悪救援WARを最小化しつつ
    //     救援登板分布を保つ）だけで較正した。
    //   ★間引き率の設計（実測知見）: 完全排除(prob=1)は逆効果＝空いたIPが最小登板の別の弱い救援へ集中し
    //     "被害の付け替え"で最悪WARがかえって悪化（-6級）する。0.6 前後の"確率間引き"が最善で、
    //     早めの検出(CurrBF小)とあわせて破綻救援のIP膨張を抑えつつ健全な救援へ負荷を薄く分散する。
    //     6seed×60年で 最悪救援WAR -5.27→-4.34 / WAR<-4 の season 17→1（登板数王・SV/HLD分布は健全維持）。
    relieverGuardPriorRA9: 5.5, // 前年の観測RA9がこれ以上＝救援として破綻の前歴（リーグ平均救援RA9≈4.2）
    relieverGuardPriorBF: 100, // 前年の最低対戦打者数（少なければ前歴と見なさない＝一時起用/新人を守る）
    relieverGuardCurrRA9: 6.0, // 当年の観測RA9がこれ以上（前歴＋当年不振の二段確認・当年持ち直しは対象外）
    relieverGuardCurrBF: 45, // 当年これだけ投げてなお不振なら間引き開始（早期検出でIP膨張を抑制。序盤小標本は不作動）
    relieverGuardExcludeProb: 0.6, // 該当救援を当日可用から外す確率（完全排除でなく間引き＝負荷を分散し登板分布を保つ）
  },

  // 休養（S3 日次スタメンAI。正捕手100-135試合へ）
  rest: {
    // R3再較正: 0.085→0.070。試合中の故障を導入したことで正捕手が年に数試合〜数十試合を
    //   故障で欠くようになり、休養と故障の二重取りで先発数が帯(100-135)の下限を割った。
    //   「休ませる」ぶんを減らし、欠場の主因を休養から故障へ移す（現実の正捕手に近い）。
    catcherRestProb: 0.068, // 捕手の休養基本率/試合 ※S5較正（正捕手100-135試合へ。名前プール確定較正: 99.8試合と帯下限を掠めたため微減）
    fielderRestProb: 0.026, // 野手の休養基本率/試合 ※S5較正
    streakW: 0.004, // 連続出場1試合ごとの休養率加算
    benchWoba: 0.295, // 不振ベンチの発動水準（観測ベース混合評価＋レンジ項がこれ未満・S5較正）
    slumpBenchProb: 0.75, // 不振ベンチの発動率/試合（捕手は通常対象外・S5較正）
    // 捕手は希少性・リード継続性から通常の不振ベンチ対象外だが、「壊滅的」水準（benchWobaより
    // 更に下＝wRAA最悪級×守備破綻）に限り控えと出場を分け、WAR-3級の定着を防ぐ（原則2「WAR-6の根絶」）。
    catcherDisasterWoba: 0.258, // 捕手の壊滅判定（混合評価＋レンジがこれ未満で作動）
    catcherDisasterBenchProb: 0.4, // 壊滅捕手の休養率/試合（完全ベンチでなく控えと分担＝正捕手枠は保つ）
  },

  // 投手疲労・可用性（S3。連投制限・中6日の基盤）
  fatigue: {
    maxConsecDays: 2, // 2連投まで（3連投禁止）
    prevDayPitchLimit: 21, // 前日この球数以上→当日不可 ※S5較正（SV/HLD/登板数の負荷分散）
    starterRestDays: 5, // 先発は中5日以上
    // R3 代替先発: 故障でローテが枯れた日に立てるブルペン投手の必要休養日数（連投明けを避ける）。
    spotStarterRestDays: 2,
  },

  // 観測成績ベース起用（S3 usage.mjs）。三層構造: 真値は直接見ない（観測statline＋スカウトノイズ）。
  usage: {
    reviewInterval: 25, // 見直し間隔（試合）
    trustPA: 80, // 観測wOBAの信頼度加重の半飽和PA（少PAは回帰）※S5較正
    scoutSd: 5, // スカウト打撃評価ノイズのSD（rating単位・scoutSeed基準）※S5較正
    // D1-3（三層構造の徹底）: 守備の起用評価(defEval/rangeEval)も真値の無ノイズ参照をやめ、
    //   打撃scoutEvalと同様に scoutSeed由来の決定論ノイズ（球団が守備を読み違える成分）を付与する。
    //   1選手につき単一のノイズを rangeEval と def[pos] 双方へ一貫適用（読み違えは首尾一貫）。
    //   ⚠️WAR下限の門番（破局/典型）を壊さない小さめのSD。0で完全無効（旧＝真値参照）。
    scoutDefSd: 3, // スカウト守備評価ノイズのSD（rating単位・scoutSeed基準・§D1-3）
    swapMargin: 0.005, // レギュラー入替に要する実効wOBA差 ※S5較正
    catcherSwapMargin: 0.04, // 捕手のみ厚め（リード面の継続性＝正捕手100-135試合の門番・S5較正）
    platoonMargin: 0.005, // プラトーン入替に要する実効wOBA差
    scoutWobaPerPt: 0.0035, // スカウト打撃評価(50中心rating)1pt → wOBA換算
    defWobaPerPt: 0.0005, // ポジション守備評価(習熟項)1pt → wOBA換算（同一ポジション内比較のみ・S5較正）
    rangeWobaPerPt: 0.0015, // レンジ評価(50中心)1pt → wOBA換算（UZR産出に比例する成分・S5較正）
    platoonWobaPenalty: 0.014, // 同利き手の実効wOBA減（相手先発でのプラトーン入替判断用）
    promoteStep: 0.45, // 見直しごとに挑戦者の先発シェアを増やす幅（1.0で完全昇格＝漸進的な入替。<0.5=3回の見直しで昇格）※S5較正
    candidatesPerPos: 6, // ポジション候補 = 編成時 positionRank の上位この人数 ※S5較正
    windowMinPA: 60, // 直近フォーム窓（前々回見直し以降）の採用に要する窓内PA（未満は累積で評価・S5較正）
  },

  // 日程v2（S3 buildSchedule）: 「節」カレンダーの休日規則
  schedule: {
    maxTeamConsecDays: 6, // 連続試合日はこの日数まで（超えたら休日を挟む＝NPBの週1休の近似）
  },

  // 打順（§S3-2 打順・現代のラインナップ理論 Tango et al. "The Book"）:
  //   その日のスタメンを毎試合、観測ベースの総合打力(ov=blendedWoba)＋スタイル傾斜（出塁/長打）で
  //   各打順スロットへ再配置する（真値非参照＝三層構造）。旧実装は編成時(trueAbility)の打順を
  //   シーズン中固定していたため、不振でも1番に居座る／鈍足捕手が1番になる穴があった（本改修で解消）。
  //   セオリー: 最強3打者を1・2・4番へ／#2が最重要スロット（PA×得点機会）／#3は2死走者なしが
  //   多く相対的に軽視／出塁は1-2番・長打は2/4/5番で価値が高い。速さは現代理論では出塁の従属変数の
  //   ため独立ノブは持たない（＝出塁傾斜が担う。これが「鈍足スラッガーの1番」も同時に抑制する）。
  lineup: {
    refOBP: 0.32, // 出塁傾斜(obpTilt)の中心（≒リーグOBP）。傾斜は中心からの偏差なので厳密値でなくてよい
    refISO: 0.12, // 長打傾斜(powTilt)の中心（≒リーグISO=SLG−AVG）
    // スロット別重み（index 0..8 = 1..9番）。value(打者,スロット)= ov·ovW + obpTilt·obpW + powTilt·powW。
    // 重要度(ovW)降順にスロットを貪欲充填（決定論）＝ #2>#4>#1>#5>#3>#6… の現代的序列で最良打者から配置。
    ovW: [0.94, 1.0, 0.84, 0.92, 0.86, 0.74, 0.62, 0.5, 0.4], // 総合打力の重み（≒打席数の重要度）
    obpW: [0.6, 0.4, 0.0, -0.3, -0.3, 0, 0, 0, 0], // 出塁傾斜の適所（1-2番で加点・4-5番は純出塁型を軽く減点）
    powW: [-0.5, 0.3, 0.0, 0.6, 0.4, 0, 0, 0, 0], // 長打傾斜の適所（2/4/5番で加点・1番は長打型を減点＝鈍足スラッガーの1番抑制）
  },

  // ロスター生成の規模（F2-1・§12.1/§15）: 支配下70人＋育成10-40人/球団（NPB準拠）。
  //   一軍はこの母集団から上澄み選抜（デプスチャート）＝二軍は自然に「一軍に及ばない選手＋若手」になる。
  //   能力の生成分布そのものは従来を維持（generatePitcher/generateFielder のノブは不変）。
  roster: {
    controlledPerTeam: 70, // 支配下人数/球団（NPB=70人）
    pitchersMin: 33, // 投手数の下限（球団差 33-36。残り＝野手 34-37）
    pitchersMax: 36, // 投手数の上限
    // --- R2 年齢分布（realism_r2_age_roster_spec §2-C）: NPB実態に寄せた山型の重み ---
    //   旧実装は「コア=一様18-37／超過分=若手厚めskew2.0」で、18歳が支配下の19%（163人/840人）を
    //   占め、しかも能力が年齢と無相関（r=0.012）だったため一軍の38%が20歳以下・規定到達者の36%が
    //   20歳以下という破綻を生んでいた（ユーザー報告「初期値ができすぎ」の正体）。
    //   【出典】npb.jp 公式の支配下選手一覧を直接集計（2026年度・巨人69人/阪神69人。realism-refs
    //   の NPB_ROSTER_AGE 参照）: 18歳1人・19歳0人・20-22歳8.5人・23-25歳19.5人・26-29歳22人・
    //   30-33歳12人・34歳以上6人／平均26.9-27.1歳。下の重み＝この実測人数をそのまま年齢別へ均した値
    //   （19歳だけは n=2球団で偶然0だった可能性を考え 1.2 に均す）。重みは相対値（合計70へ正規化）。
    ageWeights: {
      18: 1.2, 19: 1.2, 20: 2.8, 21: 2.8, 22: 2.9, 23: 6.5, 24: 6.5, 25: 6.5, 26: 5.5, 27: 5.5,
      28: 5.5, 29: 5.5, 30: 3, 31: 3, 32: 3, 33: 3, 34: 2, 35: 1.5, 36: 1.3, 37: 1.2,
    },
    // 育成（§12.1）: ソフトバンク育成51人の実測（平均21.88歳＝支配下より約5歳若い）を年齢別へ均した値。
    devAgeWeights: { 18: 4, 19: 9, 20: 6, 21: 6, 22: 6, 23: 4, 24: 4, 25: 4, 26: 2, 27: 2, 28: 2, 29: 2 },
    devCountMin: 10, // 育成選手数の下限（球団の育成方針 devFocus で 10-40 に散る）
    devCountMax: 40, // 育成選手数の上限
    devPitcherShare: 0.55, // 育成に占める投手の割合（NPB育成は投手偏重の近似）
    offenseTopN: 12, // リーグ攻撃力均衡化で測る「一軍級の上位野手」数（全員合計だと育成/控えの人数差で歪む）
    // --- F2-2 出場登録29人の選抜（selectActiveRoster・編成時評価＝buildDepthChart と同輪） ---
    activePitchers: 14, // 登録29人中の投手数（ローテ6+救援8。NPBの13-15人帯の中庸。残り15人=野手）
    activeBackupCatchers: 1, // 守備8ポジ充足後に必ず確保する控え捕手数（正捕手+1＝捕手2人体制）
    farmKeepPerPos: 2, // 登録選抜が各主ポジションに二軍へ残す最低野手数（二軍のデプスチャート成立を保証）
  },

  // シーズン中の出場登録入替（F2-3 roster_moves.mjs・phaseF_spec F2-3）。
  //   三層構造: 判定は一軍/二軍の観測statline＋スカウト評価（決定論ノイズ）のみ（真値不参照）。
  //   鉄則7: 1年目は enableMoves が立たず一切不作動（simulateSeason と bit 同一を維持）。
  moves: {
    swapCooldownDays: 10, // 入替クールダウン（NPB10日ルール＝抹消日を含み10日は再登録不可・協約84条）
    ilMinDays: 4, // IL補充を出す最低の残り離脱日数（数日の離脱では登録を動かさない）
    // ★R4: 成績入替の周期。旧25試合は粗すぎた（NPBの登録抹消は随時）。10試合ごと＝年14回。
    reviewInterval: 10,
    // ★R4「ダメなら落とす」の絶対評価（旧実装は相対評価のみ＝二軍に実績十分な上位互換が居ないと
    //   打ち込まれた投手が一軍に居座り続けた。ユーザー指摘「打ち込まれて二軍に落ちている」）。
    relegateRelieverRA9: 6.3, // 救援: 観測RA9がこれ以上なら二軍へ（リーグ平均RA9は約4.3）
    relegateStarterRA9: 5.6, // 先発: 同（緩衝を厚く＝先発は簡単には外さない）
    relegateWoba: 0.288, // 野手: 観測混合評価(blendedWoba)がこれ以下なら二軍へ
    maxPitchSwapsPerReview: 2, // 1レビューで落とす投手の上限（総入替を暴走させない）
    maxFieldSwapsPerReview: 1, // 同・野手
    fieldMinPA: 50, // 野手の不振判定に要する最低PA（代打専任等の少標本では動かさない）
    perfSwapMargin: 0.008, // 野手の成績入替に要する実効wOBA差（二軍評価−レベル差割引 > 一軍評価＋これ）
    farmGapWoba: 0.02, // 二軍観測wOBAのレベル差割引（二軍の投手レベルは低い＝観測は甘く出る）
    farmMinPA: 40, // 「相対評価」で二軍野手の成績を信じるのに要する最低PA（昇格の必須条件ではない）
    pitchSwapRA9: 1.2, // 投手の成績入替に要するRA9差（一軍RA9 −（二軍RA9＋割引）> これ）
    farmGapRA9: 0.7, // 二軍観測RA9のレベル差割引
    pitchMinBF: 25, // 救援の不振判定に要する最低対戦打者数（R4: 40→25。数試合打ち込まれれば抹消）
    farmPitchMinOuts: 45, // 「相対評価」で二軍投手の成績を信じるのに要する最低アウト数（昇格の必須条件ではない）
    // IL補充の球団AI評価（callupScore = evaluateProspect＋二軍観測の加点。観測＋スカウト）
    callupWobaW: 250, // 二軍観測wOBA偏差 → 球団AI評価点の換算（野手）
    callupRa9W: 6, // 二軍観測RA9偏差 → 球団AI評価点の換算（投手）
    callupRa9Ref: 4.5, // 二軍観測RA9の基準（これより良ければ加点）
    callupTrustPA: 80, // 二軍観測の信頼度半飽和PA（野手・少PAはスカウト評価優位）
    callupTrustOuts: 90, // 同（投手・アウト数）
    // ★R4: 守備・走塁の観測を査定に入れる（旧実装は打撃/防御率しか見ず、守備の上手い二軍野手が
    //   永遠に上がってこなかった／守備で稼ぐ一軍野手が打撃だけで落とされていた）。
    callupDefW: 0.9, // 二軍の観測守備(UZR)＋走塁(BsR) 1得点あたりの球団AI評価点
    defTrustInnings: 200, // 守備観測の信頼度半飽和イニング（少イニングのUZRは信じない）
    defWobaMinPA: 120, // 守備/走塁の得点をwOBA相当へ割るときのPA下限（少PAでの発散を防ぐ）
    // --- §req_20260708: ローテ投手の成績入替（旧実装はローテ全除外＝不振先発が固定化する欠陥） ---
    rotationMinBF: 100, // ローテ投手の判定に要する最低対戦打者数（救援pitchMinBF=40より緩衝を厚く＝数試合の不調では動かさない）
    rotationSwapRA9: 1.8, // ローテ入替に要するRA9差（救援pitchSwapRA9=1.2より大きく＝先発は簡単には外さない）
    rotationStarterScoreW: 0.02, // 昇格候補選定でstarterScore(先発適性・真値)を軽く加点する重み
    // --- §req_20260708: 育成→支配下の季節中昇格（旧実装は年1回オフのみ＋同型引退枠待ちで塩漬け）。
    //     NPB実務（支配下登録期限=例年7/31）に合わせ、シーズンの一定割合を過ぎたら昇格を締め切る。
    farmPromoteDeadlineFrac: 0.72, // 昇格を受け付ける season日数の割合（開幕からこの割合まで）
    // --- 案B（thyroxin/research/position_versatility_research_20260724.md Part2）: ---
    //     昇格/降格の適格判定を野手の primaryPos 完全一致から spectrumDistance<=1 へ拡張する。
    //     監査結論: 全昇格経路が primaryPos 完全一致ゲート＝隣接ポジションの当たり選手が候補
    //     プールに入らない（60チーム年中38でミスマッチ）。既定trueで即座に候補プールを広げる
    //     （false=旧来の完全一致ゲートに戻すロールバックスイッチ・較正/多年テストが壊れた場合の避難路）。
    crossPosPromotion: true,
    // 跨ぎ昇格の守備フィット減点（callupScore）。targetPos が primaryPos と異なる（=距離1での
    // 代替昇格）候補は、対象ポジションの観測守備適性（スカウトノイズ込み・obsTool）が
    // crossPosFitRef を下回るぶんだけ減点される＝「守れなくはないが本職ではない」の一段落ちを
    // 再現し、同ポジ候補より明確な優位（打撃/総合の差）があるときだけ跨ぎ候補が勝てる不等式にする
    // （監査の案C相当。LFの鈍足がCFへ押し込まれる逆流の抑制）。非主ポジのpositionProfはフラット
    // 抽選(平均24・研究レポートPart2実装確認)なのでcrossPosFitRef=40は「主ポジ並みではないが
    // 二軍のユーティリティブースト(35%抽選・平均48)が乗っていれば減点ゼロ」の水準。
    crossPosFitW: 1.2, // 不足1ratingあたりの減点（callupScoreのevaluateProspectスケールに対する重み）
    crossPosFitRef: 40, // これを下回る観測跨ぎ適性1ratingごとに減点開始
  },

  // 編成・打順（S1 buildDepthChart v2。§S1-3）
  depth: {
    posToolW: 0.4, // positionRank: 守備素材(Range,50中心)の重み（習熟=1基準）
    posBatW: 0.2, // positionRank: 打撃(hitScoreを50中心スケール化)の重み
    leadoffSpeedW: 1.0, // 1番選定: speed(50中心)の加点重み
  },

  // 走塁（2-4 wSB, §6）: 盗塁の試行・成否を 走者Steal/Speed × 投手Hold × 捕手Arm から生成。
  // gate系はS2の采配ゲート（監督stealTend×状況。§S2-6）。
  steal: {
    attemptBase: 0.11, // 一塁走者が二塁を狙う基本試行率（機会あたり）
    attemptSlope: 0.45, // 走者の積極性感度（Steal/Speed）
    successBase: 0.72, // 基本成功率（損益分岐~70%近辺・NPB）
    stealSlope: 0.32, // 成功率への走者寄与
    holdSlope: 0.28, // 投手クイックによる抑止
    armSlope: 0.3, // 捕手肩による抑止
    tendW: 0.4, // 監督stealTend(50中心)の試行logit感度
    gateBigDiff: 5, // 大差の目安（±この点差以上では走らない）
    gateBigDiffLogit: 1.6, // 大差時の試行logit減
    gateStrong2OutLogit: 0.8, // 2死×強打者（観測wOBA上位）での自重logit減
    // --- §tactics_re: 損益分岐サニティゲート（stealLogitAdjust。全面置換ではなく追加のロジット減点） ---
    breakevenGapGate: 0.05, // breakeven−推定成功率 がこれを超えたら減点開始（僅差は無視）
    breakevenLogit: 3.5, // gate超過分1.0あたりのlogit減点（実際の超過幅は0〜0.3程度・下記reTable参照）
  },
  // 走塁 run値（§6）。NPB寄り: SB≈+0.19 / CS≈−0.38。UBRは走者Speed/IQで進塁確率を上下（2-5）。
  // ubr系はS5較正（BsR裾）。runUBR1t3b/runUBRTakerTagは§req_20260708新設シナリオの
  // run価値（文献のRE24差分実測例~0.2-0.3runsを参考にrunUBR=0.36よりやや低めで開始）。
  run: {
    // 正典 sabermetrics_glossary.md §6.2（一次: FanGraphs Library wSB）
    //   runSB = +0.2 は全シーズン固定。runCS = −(2×RunsPerOut + 0.075) の可変式で、
    //   leagueConstants が実データから導出する（lc.runCS）。ここの値はリーグ定数が無い場合の
    //   フォールバックにすぎない（テスト・単体呼び出し用）。
    runSB: 0.2, runCS: -0.4, ubrSlope: 0.007, runUBR: 0.36,
    runUBR1t3b: 0.28, // 単打での一塁→三塁進塁（成功時の限界価値）
    runUBRTag: 0.28, // タッグアップでの二塁→三塁進塁
    // --- R1（realism_r1_baserunning_spec）: ゴロ進塁・タッグアップ深さ依存化の新ノブ ---
    gbScore3: 0.55, // 三塁走者がゴロアウトの間に生還する基準確率（<2死・非DP・非FC）
    gbAdv2t3: 0.35, // 二塁走者がゴロアウトで三進する基準確率（三塁空き）
    gbForceFc: 0.30, // R1ありゴロで「二塁封殺のみ（打者セーフ=FC）」になる確率（DPと排他）
    gbKillBase: 0.12, // ゴロゴーの本塁憤死確率（内野処理・肩補正なし）
    adv2OutBonus: 0.18, // 2死時の追加進塁ボーナス（打球と同時スタート）
    tagMinDistM: 58, // タッグアップ試行に必要な最低飛距離（内野フライ犠飛の門番）
    sfBase: 0.62, // 犠飛タッグアップ基準確率（sfPivotM地点）
    sfPivotM: 85, // 〃の基準飛距離
    sfDistW: 0.012, // 飛距離1mあたりの生還確率増分
    runGbAdv3h: 0.45, // UBR新シナリオ run重み（3塁→本塁 on out）
    runGbAdv2t3: 0.20, // UBR新シナリオ run重み（2塁→3塁 on out）
    runTag3h: 0.45, // UBR新シナリオ run重み（タッグアップ本塁）
    // tag3h（本塁タッグアップ補殺）専用の基準値。深い犠飛はほぼ毎試合発生し機会数が
    // ヒット進塁系(adv2h1b等)より桁違いに多いため、共有armKillBaseのままだと外野補殺
    // リーダーがNPB帯(5-11)を大きく超過する（較正で確認・0.10共有→補殺リーダー約17）。
    tag3hKillBase: 0.045, // 本塁憤死の基準確率（リーグ平均の肩） ※較正済み
    tag3hKillSlope: 0.0013, // 肩1pt(50中心) → 本塁憤死確率の増分
    tag3hKillMax: 0.12, // 本塁憤死確率の上限
  },
  // 併殺（2-6 wGDP, §6）: GBアウト×走者一塁×2アウト未満で併殺成立。打者の足で回避。
  gdp: { base: 0.42, speedW: 0.007, runGDP: -0.42 }, // speedWはS5較正（足↔併殺回避の結線を統計的に頑健へ）

  // 守備（§7 / 全面再設計 2026-07-09）: Distance-Time モデル（Statcast OAA と同型）。
  //   正典: thyroxin/research/fielding_metrics_reference.md §2 / §11
  //   打球ごとに各野手の「アウト化確率 p」を幾何から導く。p はリーグ中立（＝catch probability）。
  //   個人の Range は実効クロージング速度 Smax に乗り、抽選側にのみ効く。
  //   ※ 旧 hitGB/hitLD/hitFB/hitPU/evHitW/timeDifficulty*/posTypicalDepthM/rangeLogitSlope は
  //     この幾何から創発するため撤去した（打球種別の安打率はもうノブではない）。
  field: {
    // --- 守備隊形（本塁原点の極座標。r=距離[m], t=spray角[deg]・負が三塁側） ---
    //     実際のMLB/NPBの平均的な守備位置に近い値。内野手は塁間より深く守る。
    positions: {
      '3B': { r: 34, t: -33 },
      SS: { r: 44, t: -16 },
      '2B': { r: 44, t: 16 },
      '1B': { r: 33, t: 35 },
      LF: { r: 88, t: -28 },
      CF: { r: 98, t: 0 },
      RF: { r: 88, t: 28 },
    },
    // --- 到達モデル ---
    smaxBase: 6.78, // リーグ平均野手の実効クロージング速度 m/s（反応後の平均速度。全力疾走9m/sより低い）※較正済み: 得点環境中立に着地
    // Range 1pt → Smax の増分 m/s（50中心）。UZRの広がりと |xwOBA−wOBA| 乖離を同時に支配する:
    //   個人差を強めるほど「守備中立の xwOBA」と「実際に上手いレギュラーが守った wOBA」がずれる。
    //   0.022 で UZR上位≈+15（FanGraphs のゴールドグラブ級）かつ |xwOBA−wOBA| ≤ 0.003 に収まる。
    //   R2再較正: 0.022→0.019。年齢構造の導入で守備素材(reaction/hands)の分散が広がり、
    //   |xwOBA−wOBA| が恒等の帯(0.003)を割ったぶんを個人差側で吸収する（UZR帯は余裕あり）。
    //   采配妥当性ゲート導入（2026-07-23 バント2ゲート＝乱数列の引き直し）で 0.0033 と帯端を
    //   1割超過→0.019→0.018（同じ機序の1ノッチ・UZR帯は依然余裕）。
    smaxPerRating: 0.017,
    //   全リーグDH制(2026-07-25 打者母集団の再構成＝乱数列の引き直し)で |xwOBA−wOBA|=0.0034 と
    //   帯端を超過→0.018→0.017（0.019→0.018 と同じ機序の1ノッチ・UZR帯は依然余裕）。
    width: 1.05, // 到達ロジスティックの幅 m/s。小さいほど p が両極化する ※較正対象
    reactionS: 0.3, // 初動までの反応時間 s
    reachM: 1.7, // グラブ＋ダイブの到達半径 m
    backPenalty: 0.35, // 後方への移動の減速（Statcastが2017年に direction を追加した理由）※較正対象
    // --- ゴロ ---
    gbSpeedFactor: 0.8, // ゴロの実効水平速度 = EV(m/s) × これ（バウンド減速込み）※較正対象
    gbMinDepth: 3, // 迎撃点がこれより手前なら処理対象外（本塁付近の当たり）
    gbMaxDepth: 50, // 迎撃点がこれより奥なら内野を抜けた
    gloveHeightM: 2.1, // 迎撃点での打球高度がこれを超えると内野手の頭上を通過
    // --- 送球アウト（Statcast infield OAA の「塁までの距離」「打者走者の足」） ---
    transferS: 0.7, // 捕球 → リリース
    throwSpeed: 32, // 送球速度 m/s
    runnerToFirstS: 4.35, // 打者走者の一塁到達 s（speed 50 = 平均）
    runnerToFirstPerRating: 0.008, // speed 1pt → 一塁到達が何秒縮むか（速い打者に内野安打が湧く）※較正対象
    throwWidth: 0.22, // 送球アウトのロジスティック幅 s

    wRange: { positioningIQ: 0.45, reaction: 0.3, speed: 0.25 }, // Range合成重み（§7.1）
    // --- run 換算（すべて MLB.com 公式 FRV glossary の固定定数・正典§2.4） ---
    runPerOutInfield: 0.75, // "1 out = .75 runs (infielders)"
    runPerOutOutfield: 0.9, // "1 out = .9 runs (outfielders)"
    runPerError: 0.5, // 失策1つ（ポジ平均との差）あたりのrun価値。UZRのErrR成分（§7.2）
    framePerInning: 0.0005, // 捕手フレーミング（per-inning近似の遺構。実際は一球ごとに runPerCall で積む）
    // --- ARM（外野送球）: 実イベントから創発させる（旧 armRunPerOpp = 真値の線形変換は撤去）。
    //   強肩は (a) 走者に自重させ（armAdvSuppress） (b) 走った走者を刺す（armKill*）。
    //   ARM run は armOpp/armAdv/armKill の生カウントからリーグ平均基準で算出（fielding.mjs）。
    ofReachM: 45, // 空中球がこの距離以上に落ちたら外野が処理する（＝外野手の返球が関与する）
    armAdvSuppress: 0.0045, // 肩1pt(50中心) → 走者の追加進塁確率をこれだけ下げる ※較正対象
    // 走った走者を刺す基本確率（リーグ平均の肩） ※較正済み（NPB外野補殺リーダー年6〜9本）。
    // realism_r1_baserunning_spec（canKillの<2→<3緩和）で「2アウト時は本塁憤死が絶対に
    // 起きない」バグを修正した結果、kill機会（2アウト時の進塁死攻防）自体がリーグ全体で
    // 大幅に増えたため、base/slopeとも半減へ再較正（外野補殺リーダーが12seed平均で帯上限11を
    // 超過。slopeも合わせて下げないと強肩上位ほど下がり幅が小さく効果が出ない）。
    armKillBase: 0.05,
    armKillSlope: 0.0015, // 肩1pt → 刺殺確率の増分
    armKillMax: 0.2, // 刺殺確率の上限
    runPerDP: 0.4, // "Double Plays 1 = .4"（旧0.45を FRV 準拠へ）
    dpShare: 0.5, // 1件の併殺を 2B・SS で分担する比率（game.mjs が双方にフル計上するため・§B3b）
    // 捕手送球: "Catcher Throwing 1 SB prevented = .65 runs, the difference between a SB (+.2) and a CS (-.45)"
    //   → 定数を置かず run.runSB − run.runCS から導出する（正典§8.4）
    runPerBlock: 0.25, // "Catcher Blocking 1 = .25"
  },
  // 対戦巡目（1-5, §3.3）: 同一投手を1試合に複数回見るほど打者有利。
  // Phase1は一律係数のプレースホルダ。球種数重み付けは 2-1/2-2 後に有効化。
  tto: {
    kPerTime: 0.025, // 2巡目以降、K の logit を巡目あたり減らす（打者は三振しにくく）
    evPerTime: 0.7, // 2巡目以降、打球EV を巡目あたり +km/h（芯を捉えやすく）
  },

  // 打球生成(1-2)＋結果解決(1-3)。EV/LA/方向を先に作り、球場ジオメトリで結果を導く（§3.2）。
  // ※ bb 群は予備較正済み（5リーグ平均で AVG.258/OBP.323/SLG.401/HR118/ERA3.85/得点4.22、
  //    かつ分布の裾も 打率王.348/HR王47 まで圧縮=M4対応。RBI王~145は満員打線ゆえ構造的に高め）。
  bb: {
    // EV(打球速度 km/h)
    evBase: 139.46, // 平均打球速度の中心 ※D1-2: 同利きEV罰則強化(-1.2→-2.5)でリーグ平均EVが下がった分を全体で再センタリング（139→139.55・同利き相対差は不変）。
    //   0.9.1-pitchband: 帯再配分＋chase現実化＋swingEyeW圧縮で沈んだ得点環境(AVG/OBP/OPS)を全体で再センタリング（139.55→139.75）
    //   苗字プール拡張較正（2026-07-20）: 世界引き直しで得点環境が微熱（OBP0.329/ERA3.93が帯上限超過）
    //   → 139.75→139.65 の全体再センタリングで冷ます（zoneBase調整でBB%/K%は帯内に収束済み）。
    //   名前拡張（96→368・最終プール）の引き直しで再微熱 → 139.58 で最終収束
    evPerEV: 0.355, // 打者EV適性1pt → km/h ※S5較正（裾圧縮＝野手WAR王7-9.5）
    evPerPower: 0.23, // 生体power1pt → km/h ※S5較正。F2-5: 0.2→0.23（29人選抜でHR王/HR平均比が
    //   0.32→0.31へ薄まりHR王が帯割れ→powerスプレッド拡大でスラッガーの裾を回復。野手WAR王≤9.5は維持）
    evPitchSuppress: 0.3, // 投手の被コンタクト質抑止1pt → km/h減（選択球種のcontactQuality）
    evHrSuppressW: 0.25, // 選択球種のhrSuppressが高いほどEV減（弱い打球＝M3の結線）
    evAptW: 0.26, // 打者の対該当クラス適性が高いほどEV増（§4段階1）
    evSd: 14,
    // LA(打球角度 度)
    laBase: 12.55, // 選手アイデンティティ較正（2026-07-20）: 名前キー生成への切替で較正12シードの
    //   世界が引き直され、GB%が0.479→0.488と帯上限(0.48)を超過（分布は新旧不偏を実測確認済み＝
    //   固定シード集合の有限標本差）。中心を+0.55°して GB% を帯へ戻す（感度~1.7pp/度）。
    laPerLA: 0.42, // 打者LA適性 → 角度中心シフト ※S5較正（HR王の裾はLA側で確保）
    laPitchGB: 0.22, // 投手ゴロ率 → 角度を下げる
    laSd: 23,
    // 方向(spray 度: 0=中堅, ±45=ライン)。pull適性で引っ張り側へ。
    sprayPull: 0.55,
    spraySd: 17,
    // 飛距離モデル
    carry: 0.6, // v^2/g に掛ける実効係数（空気抵抗込みの縮み）
    // --- HR分布形状（D1-1・§D1）: フェンス越え判定を「HR専用飛距離モデル」として明示化 ---
    // 幾何の distanceM（安打/長打の落下点用・carry×lift幅24・peak26）と同一入力から、フェンス
    // 越え専用の hrDist を再構成する（battedBallResult）。HR飛距離は打者power/EV/最適LAへ依存:
    //   hrDist = carry・(v²/g)・hrLift(LA;peak,conc)・evBoost(EV;ref,width,gain)
    // hrLift=適角ガウス、evBoost=飽和ロジスティック（よく捉えた強打へ上限gainまでのボーナス。
    //   線形の青天井で怪物打者が暴走するのを避ける）。hrScale は総量ノブ（門番: HR/team・HR王）。
    //
    // ⚠️D1較正の重要知見: HR王(裾)の seed窓分散は「その seed にスラッガーが生成されたか」に律速
    //   される生成側の性質。打球モデル側の集中(concを狭める/evBoostを強める)は HR/team据え置き下で
    //   平均への集中が floor seed(怪物不在)のリーダーHRを uniform な hrScale 引下げで削り、
    //   out-of-sample の窓2(seeds13-24)を 40.8→38 へ悪化させる（＝裾を伸ばすと床が抜けるトレード
    //   オフ／閾値HRの hrScale 過敏）。~15通りの探索で「HR/team∈[110,130] 下で3窓すべて帯内かつ床>40」
    //   を最も頑健に満たすのは中立設定(=baseline)。よって集中は無効化(gain=0)し、モデルは明示化のみ
    //   残す（D2パークファクターが hrDist×球場フェンスで『同じ打球がHRにも凡フライにも』を出す土台）。
    hrLaPeak: 26, // HR最適打球角度（度）。幾何のliftと一致（中立＝baseline HR挙動を維持）
    hrLaConcentration: 24, // HR飛距離ガウスの幅（度）。幾何のliftと一致（狭めると床が抜けるため中立）
    hrEvRef: 152, // 飽和evBoostの中心速度(km/h)。スラッガー帯（gain=0で現在無効＝realism調整/将来フック）
    hrEvWidth: 4, // 飽和evBoostの立ち上がり幅(km/h)
    hrEvGain: 0, // evBoostの上限（0=無効＝中立/baseline維持）。>0で「よく捉えた球」のHR飛距離を最大+この割合
    // --- フェンス高→HR実効距離（D2・§11.2）: 高い壁ほどHRに必要な飛距離が増える。 ---
    //   実効フェンス = fenceDistanceAt + hrFenceHeightW×(fenceHeightM − hrFenceHeightBase)。
    //   中立球場(4m)は差0＝baseline不変。高い壁の球場は同じ打球が凡フライ側へ（原則1: 文脈で化ける）。
    hrFenceHeightBase: 4, // 実効距離ペナルティの基準フェンス高(m)。中立と同じ＝差0
    hrFenceHeightW: 1.5, // フェンス高1m超過あたりの実効フェンス距離加算(m)
    // ⚠️ 旧「結果グリッド」(hitGB/hitLD/hitFB/hitPU/evHitW/fbHitBonusM) は撤去した。
    //   打球種別ごとの安打率は、守備隊形と打球の幾何から創発する（tuning.field の Distance-Time モデル）。
    //   正典: thyroxin/research/fielding_metrics_reference.md §11.2 / §11.4
    //   同様に timeDifficultyW/Cap・posTypicalDepthM・outfieldLDTypicalDepthM も撤去（対症療法だった）。
    // 単打/二塁打の境界。新モデルでは外野手の定位置(LF/RF=88m)より手前に置く:
    //   そこへ落ちた打球は外野手が「下がりながら/走りながら」処理するため打者を単打に留められない。
    //   ※旧86.0はDistance-Timeモデル導入前の自由ノブ。得点環境中立(SLG/OPS維持)のため76.0へ再較正。
    gapDistM: 76.0,
    tripleDistM: 94, // 二塁打/三塁打候補の深さ。これ以上の深いギャップ/ライン際球が三塁打になりうる（監査B1）
    tripleBase: 0.32, // 深いギャップ球の三塁打基本率（打者speedで上下）※較正済み
    tripleSpeedW: 0.007, // 打者speed(50中心)→三塁打率への寄与
    // 失策（ROE・§7 Hands）。インプレーのアウトが確率 errBase−(Hands−50)×errHandsW で失策になる。
    // 失策以降その回の得点は非自責（ERA<失点 の差＝未自責点を生む・較正の整合に必須）。
    errBase: 0.023,
    errHandsW: 0.0006,
    // 走者進塁の確率（UBR/EqBRR文献のシナリオ別RE24分解に準拠・§req_20260708強化）
    singleScore2: 0.43, // 単打で二塁走者が生還する確率（残りは三塁止まり）※S5較正（得点環境・rpw）
    //   F2-5: 0.6→0.54（率系(AVG/OBP/SLG)に触れず総得点だけ下げるERA再収束ノブ）。
    //   §req_20260708: 新設の1塁→3塁/タッグアップ分だけ得点機会が増えたぶんをさらに0.54→0.51で相殺
    //   realism_r1: ゴロ進塁・タッグアップ・2死ボーナスで得点機会が更に増えた分を0.51→0.43で相殺
    doubleScore1: 0.36, // 二塁打で一塁走者が生還する確率（残りは三塁止まり）※較正済み（監査B後の得点環境再収束。F2-5: 0.50→0.46・同上。§req_20260708: 0.46→0.44でERA残差を相殺。
    //   realism_r1: 同様の理由で0.44→0.36）
    // ※12seed較正でrpw/ERA/runs帯を大幅超過(4.77 vs [3.9,4.3])と判明。文献値の下限側へ寄せて引き下げ。
    singleScore1to3: 0.26, // 単打で一塁走者が三塁まで進む確率（残りは二塁止まり・文献の実測値25-40%帯の下端。
    //   R1（realism_r1_baserunning_spec）: 0.10→0.26。2死ボーナス込みで~0.44へ（旧0.10は帯を大きく下回っていた）
    tagBase: 0.14, // 外野フライ(2アウト未満)で二塁走者が三塁までタッグアップする確率・NEW
  },

  // 塁打決定の守備隊形依存化（realism_r1_baserunning_spec §6・R1b改）。
  // decideBases/expectedBases が「落下距離だけ」で無条件2Bにしていた穴（外野手の目の前の
  // ポトリが必ずツーベースになる）を、落下点・ロール線と守備隊形の関係から創発させる。
  // 前落ち球（外野手の定位置より手前）の単打/二塁打はロール線への横ズレ(dPerpMin)で決める:
  // 落下後のボールは外野手側へ転がるため、正面の前落ちは必ずカット＝単打。二塁打は
  // 横ズレの大きい真のギャップ(spray±14付近)とライン際(|spray|>35)だけ（ユーザー指摘で
  // 旧dNear判定の穴＝正面前落ちの57%二塁打が発覚し、ロール線モデルへ刷新）。
  run2b: {
    frontPerpPivotM: 10.5, // 前落ち球のロール線を外野手が遮断できる横ズレの基準（これ以下はほぼ単打）
    frontDepthW: 0.33, // 落下点が浅い（アークの1m手前ごと）ほど遮断閾値を上げる（収束の時間がある）
    //   ※pivot/depthWはSLG帯[0.39,0.41]との綱引きで較正（12→10.5/0.45→0.33でSLG 0.391→0.394へ回復。
    //     正面前落ち(dPerp=0)の2B率は閾値に依らず~1%のまま＝発端バグの根治は不変）
    frontPerpWidthM: 3.5, // 前落ち2B化ロジスティックの幅
    behindStay1Base: 0.35, // 外野手の頭上/後方(beyond>0)を抜けても、至近距離なら追いつかれ単打止まりになる基準確率
    behindStay1DistW: 0.03, // 最寄り外野手からの距離1mあたり ↑の確率減少（離れるほど単打止まりしない）
    gbLinePivotDeg: 40, // ゴロのライン際2B化ロジスティック基準角度(|spray|)
    gbLineWidthDeg: 3, // 〃 の幅
    gbEvBase: 0.55, // ゴロ2B化のEV依存係数の基準値（|spray|条件成立時に掛ける倍率の下地）
    gbEvW: 0.012, // ゴロ2B化のEV(km/h,140中心)依存の傾き
    gbTripleBase: 0.03, // ゴロ3B化の基準確率（ライン際×俊足のみ・NPBの年数本水準）
  },

  // ==========================================================================
  // 球場生成分布（D2 パークファクター・§11.2「文脈で化ける」）: 各球団本拠地に完全架空の
  //   球場ジオメトリ（両翼/中堅距離・左右非対称・フェンス高）を持たせる分布ノブ。
  //   src/generate.mjs の generatePark が消費し、generateLeague が球団ごとに1つ生成する。
  //   分布は対称（各偏差の平均0）＋リーグ内でゼロサム中心化＝リーグ平均ジオメトリ=中立球場。
  //   よってリーグ全体の得点環境は据え置き（PF平均≈100）。HR閾値の凸性ぶんだけ微増するのを
  //   hrScale の再較正で吸収する（§D2）。PF上位/下位は ±5-15% に散る。
  //   決定論: park専用のRNG系列（hashSeed(seed,'park',ti)）で引き、選手生成RNGは一切触らない
  //   （＝選手は D2 前と byte 同一。変わるのは球場を使う season シムのみ）。
  // ==========================================================================
  park: {
    baseLine: 100, // 両翼の中立距離(m)
    baseCenter: 122, // 中堅の中立距離(m)
    baseHeight: 4, // フェンス高の中立(m)
    // ⚠️D2較正: HR越えは閾値（凸）ゆえ球場個性は「リーグ平均HR/HR王」を動かす。SDを上げると
    //   凸性で HR/team が膨らみ HR王(裾)が薄まる（生成律速・D1知見）。~k=0.55 で HR/team≈121・
    //   HR王≈42・PFspread≈0.30（[0.10,0.34]）・得点環境据え置きを同時達成（8-12seed探索）。
    sizeSd: 1.4, // 球場全体の広狭（両翼＋中堅を一様シフト）偏差SD(m)
    centerSd: 2.5, // 中堅の独立偏差SD(m)（広い中堅＝中距離弾が凡フライ／狭い中堅＝伸びる）
    asymSd: 1.9, // 左右非対称の偏差SD(m)（狭い翼×引っ張り＝移籍で化ける/死ぬ）
    heightSd: 0.7, // フェンス高の偏差SD(m)
    lineClampLo: 88, // 両翼距離の下限(m)（非現実的な極端値の抑制）
    lineClampHi: 112, // 両翼距離の上限(m)
    centerClampLo: 112, // 中堅距離の下限(m)
    centerClampHi: 134, // 中堅距離の上限(m)
    heightClampLo: 1.5, // フェンス高の下限(m)
    heightClampHi: 8, // フェンス高の上限(m)
    batShare: 0.5, // PF→打者/投手補正の在宅露出率（本拠地は約半分の試合）。pfBat=1+(pfRuns−1)×これ
  },

  // ==========================================================================
  // 時代トレンドと王朝均衡（D3・§11.3「集団・時代系」・多年運用）: オフシーズン遷移で「時代」を
  //   ゆっくり動かすノブ。src/game/era.mjs の computeEra/eraSeasonConfig/teamBalanceBoost が消費。
  //   ⚠️**1年目（yearIndex=0）は完全に identity**（computeEra が全成分ゼロ・eraSeasonConfig が
  //   baseCfg を同一参照で返す）＝既存50較正は完全不変（byte一致）。ドリフトは2年目以降のみ。
  //   決定論: era は (masterSeed, yearIndex) の純関数＝live/replay で bit 一致。
  // ==========================================================================
  era: {
    enabled: true, // false でD3を無効化（全年 identity＝多年でもドリフト無し）
    // --- 得点環境の緩やかな揺れ（投高打低↔打高投低）: EV中心(bb.evBase)を正弦で上下 ---
    //   位相0の sin(2π·yi/period) ゆえ yi=0 で必ず0（year0 identity の担保）。振幅±0.8km/h で
    //   得点環境が緩やかに上下する（打高/投高の時代）。周期は世界ごとに散らす。
    offenseAmpKmh: 0.8, // EV中心の揺れ振幅(km/h)
    wavePeriod: 9, // 揺れの基本周期(年)
    wavePeriodSd: 2.0, // 周期の世界差SD(年)（位相0は保持）
    // --- 平均球速の経年上昇（新人世代が世代ごとに速くなる・約+0.5km/h/年）---
    veloPerYear: 0.5, // 新人生成の平均球速が1年（yearIndex）あたり+これ(km/h)
    veloBumpMax: 10, // 経年上昇の上限(km/h)（暴走防止）
    // --- 世代の波（ドラフト当たり年/外れ年→黄金世代）: 新人品質(rating)を年ごとに揺らす ---
    cohortSd: 1.2, // 通常年の新人品質の揺れSD(rating)
    goldenProb: 0.12, // 黄金世代（当たり年）の確率
    goldenBoost: 5.0, // 黄金世代の新人品質ボーナス(rating)
    leanProb: 0.12, // 外れ年の確率
    leanPenalty: 4.0, // 外れ年の新人品質ペナルティ(rating)
    // --- 王朝と均衡（戦力の平均回帰＝ドラフト再分配）: 弱い球団の新人を厚く ---
    balanceReversion: 40, // 勝率.500未満ぶん(0-1)×これ＝新人 rating boost（弱い球団ほど良い人材）
    balanceMaxDev: 0.15, // 反映する勝率不足の上限（.35以下は同扱い＝暴走防止）
  },

  // ==========================================================================
  // 加齢・成長カーブ（C2a・§10.1-10.3 / §12.4）: オフシーズンに trueAbility を動かすノブ。
  // src/game/aging.mjs の applyAging が消費する（1年目レギュラーシーズンには一切効かない
  // ＝加齢は2年目以降のみ。既存50較正指標は完全不変）。
  //
  // モデル（能力ごとに「成長→維持→衰え」の3相）:
  //   growEnd = peakAge + peakShift  … ここまで毎年 +grow（若手は成長係数 gm で幅を持つ）
  //   onset   = peakAge + declineOffset … ここから毎年 −decline×declineRate×(1+accel×経過年)
  //   peakShift/declineOffset を能力タイプで振り、§10.1 の「早落ち/遅くまで残る/むしろ伸びる/
  //   加齢に強い」を構造から出す。declineRate は個体差（§10.2・generate で球速/走力相関で既引き）。
  // ==========================================================================
  // ==========================================================================
  // R2 成熟度（generate が aging と同一カーブで「年齢に応じた現在能力」を作る）
  //   §10.1 / realism_r2_age_roster_spec §2-A,B,D
  //
  // 核心: generatePitcher/generateFielder が引くのは **ポテンシャル（成長終端＝peak時の能力）**。
  //   現在能力 = ポテンシャル + baseLift + survivorBonus(age) + maturityDelta(age)（applyMaturity）。
  //   maturityDelta は aging.profiles の grow/decline をそのまま逆算に使う＝生成と加齢が同一関数に
  //   なり、「生成された28歳」と「18歳から育った28歳」が同分布になる（内部整合）。
  //   旧実装は年齢と能力が無相関（18歳の平均能力＝30歳）で、加齢側はそれに合わせて成長を
  //   ほぼゼロ（power grow 0.2/年＝9年で+1.8pt）まで潰してあった（生成の誤りに加齢を合わせた形）。
  // ==========================================================================
  maturity: {
    survivorFromAge: 25, // これを超える年齢からポテンシャルに生存バイアス加点（§10.6）
    survivorSlope: 0.4, // 1歳あたりの加点(rating)。支配下に残る高齢者＝弱い個体が淘汰された後の姿
    baseLift: 2.0, // ★較正の主ノブ: 一軍登録29人の能力分布を年齢構造導入前と一致させる中心化
    veloPerRating: 0.45, // baseLift/survivorBonus を球速へ写す換算（生成sd 4.5km/h ÷ rating sd 10）
    // 野手の power/ev への追加加点（R2較正）: 年齢構造の導入で一軍の平均年齢が 23.9→27.9歳へ上がり、
    //   power/ev は decline が最も速い軸（1.55/年）なのでリーグ長打力だけが構造的に不足する
    //   （SLG/OPS/HR王 が同時に帯を割る）。長打だけを戻すノブ（contact 系には効かせない）。
    powerLift: 1.97,
  },

  aging: {
    youngAge: 25, // これ未満を「若手」とする（成長を高分散・bust厚めで出す＝§10.3）
    driftSdYoung: 1.6, // 若手の年次ノイズSD（能力1軸あたり・rating）
    driftSdOld: 0.9, // ベテランの年次ノイズSD
    growthVarYoung: 0.35, // 若手の成長係数 gm の分散（点でなく幅で・TINSTAAPP）
    bustProb: 0.15, // 若手が「伸び悩む」確率（下方の裾を厚く＝bust多め）
    bustMag: 0.9, // bust 時に成長係数 gm から引く量（負に振れれば真値が退行）
    growthMultMin: -0.6, // gm の下限（bust で退行しうる）
    growthMultMax: 2.4, // gm の上限（覚醒的な急成長の上振れ）
    declineAccel: 0.12, // 衰えの加速（onset 超過1年ごとに衰え幅を増やす＝終盤の急落）
    // R7（決定1）: 高卒新人の期限付き未成熟負債（market.draft.youthDebt*）の毎年の返済額(rating)。
    //   全軸へ均等加算し career.youthDebt を0へ収束させる（高卒の負債-3.2なら約4年で完済）。
    youthDebtRepayPerYear: 0.9,

    // 球速（km/h 実数・レーティングと別枠）: 加齢で落ちる。高球速×高declineRate ほど早く落ちる。
    velo: {
      grow: 0.5, // 若手のうちは伸びる（高卒入団145→数年で150 の実感。R2: 0.3→0.5）
      peakShift: -1, // 球速のピークは早い
      declineOffset: 2, // ピーク+2 から本格的に落ちる
      decline: 0.6, // 1年あたりの低下（×declineRate×加速）
      driftSdYoung: 0.6,
      driftSdOld: 0.4,
      min: 130,
      max: 165,
    },

    // 能力タイプ別プロファイル（§10.1）。grow=成長幅 / peakShift=成長終端の後ろズレ /
    //   declineOffset=衰え開始の後ろズレ / decline=衰え幅。未登録キーは default。
    //
    // ★R2 再設計（realism_r2_age_roster_spec §2-E）:
    //   【旧状態】grow をほぼゼロまで潰してあった（power/ev 0.2＝18→27歳の9年で +1.8pt）。
    //     理由は §11.3 Bug1（多年運用で得点環境が単調インフレ: SLG+16%/HR+71%/ERA+32%）の抑止。
    //     だがインフレの真因は加齢ではなく **generate が年齢と無関係に「完成品」を生成していたこと**
    //     （18歳の平均能力＝30歳・r=0.012）。完成品に成長を足せば当然インフレするので、
    //     加齢の成長を潰して辻褄を合わせていた＝「生成の誤りに加齢を歪めて合わせた」状態。
    //     副作用として ③やきゅつく的な楽しさ（若手が育つ）が構造的に消えていた。
    //   【新構造】generate 側が applyMaturity で「年齢に応じた未成熟/衰え」を作るようになったので、
    //     加齢は **本来の成長幅** に戻す。母集団は「生成分布＝定常分布」（生成と加齢が同一カーブ）に
    //     なるため、原理的に net drift≈0 が保たれる。門番は test/game_multiyear.test.mjs（20年×
    //     得点環境の帯・単調非増加）。
    //   grow の相対比は現実の成熟パターンに従って設計し直した（旧比は power/ev を狙い撃ちで潰した
    //     歪んだ比だった）: 走力・肩・球速は早熟（18歳で既に一線級）／パワー・体の成熟・技術・
    //     制球・守備習熟は経験と体づくりで大きく伸びる。
    //   decline は旧値を維持（衰えの形は正しかった）。個体の物語（§10.3 gm・§10.4 ブレイク・
    //     §12.4 晩成/鉄人テール）はそのまま。
    profiles: {
      // 早熟・早落ち（走力/守備初動は18歳で既に完成に近く、真っ先に衰える）
      speed: { grow: 0.35, peakShift: -3, declineOffset: -1, decline: 1.2 },
      reaction: { grow: 0.5, peakShift: -2, declineOffset: 0, decline: 1.0 },
      steal: { grow: 0.8, peakShift: -2, declineOffset: 0, decline: 0.9 }, // 足そのものでなく技術＝経験で伸びる
      // パワー/EV（得点環境の主動因）: 体の成熟＝若手が最も伸びる軸。以後は速やかに衰える（山型）。
      power: { grow: 1.4, peakShift: 0, declineOffset: 0, decline: 2.0 },
      ev: { grow: 1.4, peakShift: 0, declineOffset: 0, decline: 2.0 },
      // 選球眼（加齢に強く、むしろ伸び続ける・§10.1）
      eye: { grow: 1.0, peakShift: 1, declineOffset: 3, decline: 0.25 },
      // コンタクト・三振耐性（技術＝経験で伸び、peak 以後は明確に悪化）
      contact: { grow: 1.3, peakShift: 0, declineOffset: 0, decline: 1.6 },
      vsFastball: { grow: 1.1, peakShift: 0, declineOffset: 1, decline: 1.3 },
      vsBreaking: { grow: 1.2, peakShift: 1, declineOffset: 1, decline: 1.3 }, // 変化球対応は経験の産物
      // LA最適化（晩成の主軸。高 gm×高 peakAge の少数テールから「遅れて開花」が出る）
      la: { grow: 1.0, peakShift: 1, declineOffset: 1, decline: 1.45 },
      pull: { grow: 0.3, peakShift: 1, declineOffset: 1, decline: 0.4 },
      // 技術・IQ（経験で伸び加齢に強い）。鉄人（高制球・低 declineRate）は個体テールから出る。
      control: { grow: 1.2, peakShift: 2, declineOffset: 5, decline: 0.55 }, // 石川雅規型（鉄人）はテール
      positioningIQ: { grow: 1.0, peakShift: 1, declineOffset: 3, decline: 0.35 },
      baserunIQ: { grow: 1.0, peakShift: 1, declineOffset: 3, decline: 0.35 },
      framing: { grow: 1.0, peakShift: 2, declineOffset: 4, decline: 0.4 },
      blocking: { grow: 1.0, peakShift: 2, declineOffset: 4, decline: 0.5 },
      gbRate: { grow: 0.3, peakShift: 2, declineOffset: 3, decline: 0.4 },
      hold: { grow: 0.6, peakShift: 2, declineOffset: 3, decline: 0.5 },
      positionProf: { grow: 1.2, peakShift: 2, declineOffset: 4, decline: 0.5 }, // 守備習熟は経験で伸び緩く落ちる（山本泰寛型）
      // 球種の質（出し入れは技巧で残る）。R2較正: grow は K%↔Contact% のトレードオフの均衡点。
      //   growEnd(=peak+2) が一軍投手の平均年齢より後ろにあるため、grow を上げるとリーグの投手が
      //   構造的に「未成熟」側へ寄り Contact% が帯上限を割り、下げると投手が強くなり K% が超過する。
      //   （peakShift の前倒しは投手を一段強くしてしまい打撃3指標が同時に帯を割ったため不採用）
      pitchStuff: { grow: 0.85, peakShift: 2, declineOffset: 3, decline: 0.6 },
      // 中間（肩=早熟／手・スタミナ=育成で伸びる）
      arm: { grow: 0.5, peakShift: 0, declineOffset: 2, decline: 0.8 },
      hands: { grow: 0.9, peakShift: 2, declineOffset: 3, decline: 0.6 },
      stamina: { grow: 1.0, peakShift: 1, declineOffset: 2, decline: 0.7 },
      default: { grow: 0.8, peakShift: 1, declineOffset: 4, decline: 0.6 },
    },
  },

  // ==========================================================================
  // 故障ハザード（C2b・§10.5）: オフシーズンに「その年に故障したか」を確率事象で決める
  //   （予測でなくハザード）。年齢・ポジション（捕手は壊れる）・投手の投球負荷（球速）・
  //   故障歴（最大リスク／再発）で確率を上下。src/game/injury.mjs の applyInjuries が消費。
  //   後遺（復帰後の一時的能力減）と非対称性（投手の大怪我＝将来リスク＋球速低下の始まり）を持つ。
  //   1年目レギュラーシーズンには一切効かない（故障判定は2年目以降のオフシーズンのみ）。
  // ==========================================================================
  // ==========================================================================
  // 故障（§10.5・★R6 文献調査に基づく再設計）
  //   構造: h = base × durability倍率 × 年齢倍率(役割別) × 投球負荷 × (1 + recurW × 直近故障の残債)
  //   ★故障歴の「累積加算」は撤去した。Wei et al., Am J Epidemiol 2011: 故障回数の用量反応は
  //     個体の潜在的脆弱性(frailty)を調整すると消失する＝累積は因果でなく選択効果。
  //     直近故障の残債は指数減衰（幾何級数で収束）＝人為的な上限(cap)が要らない。
  // ==========================================================================
  injury: {
    base: 0.055, // 基本ハザード（平均的な選手・1シーズンあたりのスケール）
    cap: 0.60, // 安全弁（構造上ここには届かない。暴走検知用）

    // --- ① 潜在durability（生涯不変の真値・trueAbility.career.durability・50中心sd10） ---
    //   Karnuta 2020(OJSM, 13,982選手年): 既往故障は最重要変数だが予測力は AUC 野手0.76/投手0.65
    //   ＝「有意だが中程度」。個体差は実在するが平均回帰が強い → スプレッドは控えめに。
    durabilityPerPt: 0.020, // 50から1pt低いごとにハザード×(1+これ)
    durabilityMultMin: 0.55, // 鉄人（durability 80）でも 0.55倍まで
    durabilityMultMax: 1.75, // ガラス（durability 20）でも 1.75倍まで（AUC相当の予測力に収める）

    // --- ② 直近故障の残債（指数減衰・累積しない） ---
    recurW: 0.75, // 残債1単位あたりのハザード増（直近故障で約1.75倍＝RR 2前後に整合）
    recurTauYears: 1.2, // 減衰の時定数（半減期≈0.83年）。Arthur LASSO 0.18/0.10/0.02 からの導出
    recurMaxYears: 3, // これより古い故障は寄与ゼロ（3年前の係数0.02≒無視できる）
    recurMajorW: 1.8, // 重症の残債は重い（UCL再建の revision は37%が3年以内）

    // --- 年齢（投手はU字・野手は緩やかな線形） ---
    age: {
      pitcherTroughAge: 24, // 投手のリスク最小年齢（谷）
      pitcherYoungPerYear: 0.05, // 谷より若い1歳ごとに +5%（21-22歳は高リスク）
      pitcherOldPerYear: 0.035, // 谷より上の1歳ごとに +3.5%
      fielderRampAge: 25, // 野手はここから
      fielderPerYear: 0.055, // 1歳あたり +5.5%（実測 欠場日数 +6.4%/歳）
    },

    // --- 役割・投球負荷 ---
    pitcherMult: 1.38, // 投手の上乗せ（件数比 1.0-1.3:1＝NPB実測。man-days比はもっと大きいが
    //                    それは「1件あたりの離脱が長い」から＝部位テーブル側で表現する）
    //   スペクトラム昇格導入（2026-07-24）でロスター構成が変わり件数比0.849と帯下端(0.85)を
    //   微割れ→1.35→1.38の1ノッチで再収束（帯中央寄りへ）。
    veloRef: 150, // これを超える球速から負荷加算（R6: 148→150）
    veloPerKmh: 0.008, // ★R6: 旧 0.004/km/h（ハザードへの直接加算）から「倍率」へ変更し実質半減。
    //   Fleisig 2025（前向きn=305）で球速とUCLは HR=1.02＝有意差なし。支持側でも説明力7%。

    // --- 登板間隔（中4日以下はリスク増）---
    //   PubMed 39292010（2024）: 登板間隔5日超のチームは筋骨格系IL日数 IRR=0.78（22%減）。
    shortRestDays: 5, // これ未満の間隔で登板した先発は
    shortRestMult: 1.28, // ハザード×これ（IRR 0.78 の逆数≒1.28）

    // --- 部位テーブル（Posner 2011 / HITS総説 Lezak 2026 の実測から） ---
    //   share=その役割の故障に占める割合／medianDays=離脱日数の中央値（対数正規の中央値）
    //   投手は上肢67%・下肢17%（Posner）。UCL損傷の平均離脱は 274.9日（＝実質シーズン絶望）。
    sites: {
      pitcher: [
        { id: 'ucl', name: '肘内側側副靱帯', share: 0.10, medianDays: 200 },
        { id: 'elbow', name: '肘', share: 0.14, medianDays: 30 },
        { id: 'shoulder', name: '肩', share: 0.22, medianDays: 28 },
        { id: 'forearm', name: '前腕', share: 0.12, medianDays: 22 },
        { id: 'lat', name: '広背筋', share: 0.07, medianDays: 30 },
        { id: 'oblique', name: '腹斜筋', share: 0.09, medianDays: 21 },
        { id: 'back', name: '腰背部', share: 0.09, medianDays: 14 },
        { id: 'hamstring', name: 'ハムストリング', share: 0.09, medianDays: 15 },
        { id: 'knee', name: '膝', share: 0.05, medianDays: 30 },
        { id: 'other', name: 'その他', share: 0.03, medianDays: 14 },
      ],
      fielder: [
        { id: 'hamstring', name: 'ハムストリング', share: 0.20, medianDays: 15 },
        { id: 'knee', name: '膝', share: 0.13, medianDays: 30 },
        { id: 'ankle', name: '足首', share: 0.08, medianDays: 20 },
        { id: 'groin', name: '鼠径部', share: 0.07, medianDays: 18 },
        { id: 'oblique', name: '腹斜筋', share: 0.10, medianDays: 21 },
        { id: 'back', name: '腰背部', share: 0.10, medianDays: 14 },
        { id: 'shoulder', name: '肩', share: 0.11, medianDays: 25 },
        { id: 'wrist', name: '手首・手指', share: 0.10, medianDays: 20 },
        { id: 'elbow', name: '肘', share: 0.05, medianDays: 30 },
        { id: 'concussion', name: '脳震盪', share: 0.03, medianDays: 12 },
        { id: 'other', name: 'その他', share: 0.03, medianDays: 14 },
      ],
    },
    siteRecurBias: 2.6, // 過去に壊した部位は引かれやすい（同一部位の再発。ハム再受傷率 16.3%）

    // --- 離脱期間（部位別の対数正規） ---
    durationSigma: 0.66, // 右裾の重さ（平均/中央値 ≈ 2.6 を再現・Camp 2018）
    daysPerGameLost: 1.05, // 日数 → 離脱"試合数"（日程には全休日がある）
    minGamesLost: 9, // 最小離脱（NPB: 抹消日を含み10日は再登録不可＝協約84条）
    majorGamesThreshold: 75, // これ以上の離脱を「重症（シーズン規模）」とする

    // --- 後遺（★R6: 「率は戻るが出場量は戻らない」・Camp 2018 / Lansdown & Feeley）---
    //   TJ手術後: K%/BB%/FIP は **有意差なし（戻る）**。球速は −0.7mph（35歳以上は −2.9mph）。
    //   一方 **登板数 120.7→72.6・投球回 338.1→223.6 は術後3年平均でも回復しない（P<.001）**。
    //   → 旧実装の「power/ev/contact を恒久的に削る」は誤り。**削るのは"出場量"の側**。
    aftereffect: {
      minorNone: true, // 軽症は恒久的な能力低下を残さない（率は戻る）
      majorStamina: 5.0, // 重症の投手: スタミナ恒久減（＝登板数/イニングが戻らない）
      majorVeloKmh: 1.1, // 重症の投手: 球速 −1.1km/h（≈ −0.7mph）
      majorVeloKmhOld: 4.7, // 35歳以上は −4.7km/h（≈ −2.9mph）
      oldAge: 35,
      majorLowerBodySpeed: 3.0, // 重症の下肢（ハム/膝/足首/鼠径）: 走力・初動の恒久減
      majorLowerBodyReaction: 1.5,
      lowerBodySites: ['hamstring', 'knee', 'ankle', 'groin'],
      // 肩の重症はキャリア長を削る（関節唇修復後: 投手 2.3年 vs 対照 5.8年）→ 引退圧へ
      shoulderRetireBias: 0.10,
    },

    // --- 試合中の発生（露出イベントごと・R3） ---
    inSeason: {
      enabled: true,
      refHazard: 0.055, // 倍率の正規化基準（= base。平均的な選手が下の base 確率になる）
      perPA: 0.00034, // 打者: 1打席（スイング・全力疾走。走塁もここに含める）
      perBF: 0.00055, // 投手: 対戦打者1人（試合外の投げ込み消耗もここに吸収）
      perFieldPlay: 0.00043, // 野手: 自分が処理した打球1つ
      perCatcherPA: 0.000016, // 捕手: 守備側1打席（頻度は割増さない＝Guy 2015）
      daysPerGame: 1.2, // gamesLost → day 換算のフォールバック（実際は日程から実測）
    },
  },

  // ==========================================================================
  // ブレイクイベント（C2b・§10.4/§11.1）: 低確率で能力が階段状にジャンプする離散イベント。
  //   上方=球種習得（列が生える千賀型）/覚醒（真値ジャンプ）/EVジャンプ（板山・用具型）、
  //   下方=制球崩壊（イップス）/燃え尽き/故障明けの別人化。
  //   §11.1: 上方だけだとリーグがインフレするので下方>上方を既定とする（バランス上ほぼ必須）。
  //   src/game/breakout.mjs の applyBreakouts が消費。1年目には効かない（2年目以降のオフのみ）。
  // ==========================================================================
  breakout: {
    upBase: 0.010, // 上方ブレイクの基本確率/オフシーズン
    downBase: 0.014, // 下方ブレイクの基本確率（上方より厚く＝インフレ抑止）
    youngAge: 24, // これ以下は覚醒（上方）が出やすい
    youngUpMult: 1.6, // 若手の上方確率倍率
    burnoutAge: 32, // これを超えると燃え尽き（下方）が出やすくなる
    burnoutPerYear: 0.002, // burnoutAge 超過1歳ごとの下方確率加算
    postInjuryDown: 0.15, // 直近オフに大怪我があると下方（別人化）が跳ね上がる
    newPitchShare: 0.5, // 投手の上方のうち「球種習得（列が生える）」になる割合。残りは覚醒
    yipsShare: 0.5, // 投手の下方のうち「制球崩壊（イップス）」になる割合。残りは燃え尽き
    jump: 8, // 覚醒/燃え尽きの階段幅（レーティング）
    veloJump: 2.5, // 覚醒時の球速ジャンプ(km/h)
    controlCollapse: 14, // イップス時の制球低下幅
    postInjuryMag: 6, // 故障明け別人化の追加低下幅
  },

  // ==========================================================================
  // H3-1: 性格タグ（phaseH_fun_spec H3・fun_design_evidence §4柱4）。
  //   p.personality（8種・独立シード付与＝generate.mjs assignPersonality）が読む効果ノブ。
  //   すべて小さく・オフシーズン処理限定・期待値は動かさない（分散/確率の"形"だけ傾ける）。
  //   寡黙/闘志/クール/マイペース/リーダーはフレーバー専用＝ここに対応する数値効果を持たない。
  // ==========================================================================
  personality: {
    // ムラっ気: aging.mjs の年次ドリフトSD（drift/ageVelocity）に掛ける倍率。平均（curveDelta）は
    //   不変＝「同じ成長曲線を、より荒れた足取りで辿る」だけなので多年の中心傾向を動かさない。
    streakyDriftMult: 1.25,
    // お調子者: breakout.mjs の上方/下方確率（upP/downP）に同率で掛ける倍率。§11.1「下方≧上方」の
    //   比はそのまま保たれる（分散が増えるだけ＝インフレ方向のバイアスを追加しない）。
    showboatBreakoutMult: 1.2,
    // 練習熱心: H4（育成方針・キャンプ）の方針tiltStrengthに掛ける倍率。
    //   src/game/training.mjs の resolvePlayerTraining が消費する。
    hardworkingTrainingMult: 1.3,
  },

  // ==========================================================================
  // H4: 育成方針・キャンプ（phaseH_fun_spec H4）: 期待値保存の間接介入。
  //   src/game/training.mjs（方針の意味論・解決）＋ src/game/aging.mjs（適用）が消費する。
  //   鉄則（R7の教訓・draft_timeline_evidence 決定1）: aging.profiles.grow そのものは一切
  //   変更しない。curveDelta の「成長」summand にだけ、対象軸グループ×(1+δ)・非対象×(1−δ·w)を
  //   掛ける（w は対象/非対象の grow 総和比で自動正規化＝個体レベルで期待成長量の総和を保存）。
  // ==========================================================================
  training: {
    tiltStrength: 0.15, // δ: 軸グループ間の成長再配分の強さ（対象+δ/非対象-δ·w）
    specialSlotsPerTeam: 3, // 特別指導枠K（1球団・1年あたりの上限人数）
    specialMult: 2, // 特別指導枠のtiltMult倍率（δ・練習熱心と乗算合成）
    restDriftMult: 0.7, // rest: 年次ドリフトSDの倍率（<1で荒れを抑える）
    restDeclineMult: 0.8, // rest: 衰え(decline)の倍率（<1で衰えを緩める）
    restGrowMult: 0.8, // rest: 成長(grow)の倍率（休養のぶん今季の伸びは削れる＝トレードオフ）
    // AI球団の自動方針（teamEvalProfile由来・決定論）: wBat/wDefをそれぞれの分布の
    //   平均/SDでz化した差 zBat-zDef が閾値を超えた球団だけ batting/defense へ偏らせる
    //   （閾値を高めに取り、大半の球団は balanced のまま＝リーグ全体の較正ヘッドルームを守る）。
    //   投手は対象外（打撃/守備の方針は野手のみに適用）。
    aiTiltZThresh: 1.5,
    // ★実測知見（R7と同種の教訓）: AI球団の自動方針は「毎年・同じ選手群へ・20年間持続的に」乗る
    //   ため、たとえ効果を0.3や0.001まで弱めても test/game_multiyear.test.mjs の多年ERA帯[3.3,4.6]を
    //   非単調に破った（grid探索で確認）。原因は加齢→観測→引退/昇格判定という離散判断のフィード
    //   バックループが本質的にカオス的で、どれほど微小な摂動でも20年後には別の分岐（別の選手が
    //   生き残る/淘汰される）へ発散しうるため＝「小さくすれば安全」が成立しない（線形に減衰しない）。
    //   したがって既定値は0（AI方針の文字列自体は決定論的に算出し続けるが、加齢への実効果は
    //   厳密にゼロ＝(1+0)/(1-0*w)=1で curveDelta に一切影響しない＝既存挙動と bit 同一）。
    //   人間の明示介入（setTrainingPolicy）はテスト/calibrate/realismの自動実行経路では一度も
    //   呼ばれない（UI操作のみが積む）ため、tiltStrength自体はフルで機能する。
    aiEffectMult: 0,
  },

  // ==========================================================================
  // 引退・世代交代（C2b・§10.6）: 能力・年齢・故障から引退を確率判定し、同チーム・同枠へ
  //   新人（ドラフト相当）を1:1で補充してリーグ人口/ロスター構成を恒常に保つ。
  //   生存バイアス（弱い個体が消え、加齢カーブが平らに見える＝鉄人が自動レア化）はそのまま正しい。
  //   src/game/roster.mjs の runRetirementAndDraft が消費。能力=引退判定の「出場機会」の代理
  //   （弱い＝出番が減る＝切られる）。真の出場機会依存の戦力外は C3（§12.2）で導入。
  // ==========================================================================
  retire: {
    // ★R2 再設計: 旧 minAge=32 は「32歳未満は絶対に引退しない」＝ NPB の新陳代謝（毎年70人中
    //   8-12人が引退/戦力外で入れ替わる）に対し支配下の淘汰が 1/4 しか起きず、多年運用で
    //   ロスターが高齢化し続けていた（実測: 支配下平均年齢が 26.7→29.5歳へ漂流し、成長した選手が
    //   滞留して SLG/HR が単調インフレ）。現実の球団は「高卒5年目で芽が出なければ切る」。
    minAge: 24, // これ未満は引退しない（育成期間は守る）
    hardAge: 42, // これ以上は必ず引退（超高齢の打ち切り）
    rampAge: 31, // この年齢超過で引退確率が加齢加算され始める
    agePerYear: 0.075, // rampAge 超過1歳ごとの引退確率加算（late-30s の淘汰を効かせる）
    abilityRef: 47, // 「並」の総合力（この未満は引退圧が増す＝出場機会の代理）
    abilityPerPt: 0.028, // abilityRef 未満 1pt ごとの引退確率加算
    // R2: 若手は「現在能力」ではなく「伸びしろ」で評価される（maturity 導入で若手の現在能力は
    //   構造的に低い＝これが無いと球団が将来のエースを能力40と見て切ってしまう）。
    //   peakAge までの残り年数 × これ を総合力に加算してから引退圧を測る（TINSTAAPP は
    //   「伸びなかった中堅(25-28)が淘汰される」形で出る＝現実の戦力外通告の姿）。
    youthCreditPerYear: 0.9,
    injuryPerHist: 0.030, // 故障歴1件ごとの引退確率加算
    eliteRetain: 0.018, // abilityRef 超過 1pt ごとの引退確率減（鉄人ほど残る＝生存バイアス→40代レア化）
    base: 0.022, // 基本引退確率（判定対象年齢での下駄）
    cap: 0.92, // 引退確率の上限
  },

  // ==========================================================================
  // 編成市場（C3a・§13/§15/§12.1）: 球団AI評価の球団差＋ドラフト＋育成/支配下二層。
  //   1年目レギュラーシーズンには一切効かない（市場はオフシーズン遷移＝2年目以降のみ）。
  //   src/game/market.mjs が消費。三層構造の原則: 球団AIは trueAbility を直接見ず、
  //   「観測ツール＋スカウトノイズ」を球団固有の重みで評価する（＝わざと不完全に間違う）。
  //   歪んだ球団評価と真価値の差分が「宝の泉」＝市場の非効率（守備/位置の過小評価を仕込む）。
  // ==========================================================================
  market: {
    // ★R2: ドラフト新人のポテンシャル補正（負値）。球団は surplus 付きプールから「自評価の最良」を
    //   指名するため、指名された新人のポテンシャルはプール平均より構造的に高く出る（選抜の上澄み）。
    //   補正しないと毎年リーグへ「平均より強い個体」が注入され続け、多年で能力が単調インフレする
    //   （実測: 無補正だと15年で一軍EV +1.5pt → SLG .402→.433）。生存バイアス（弱個体の引退）と
    //   合わせた正の圧力を打ち消し、リーグ平均を定常に保つノブ（§11.3 の「有界な揺れ」の担保）。
    rookiePotentialLift: -2.5,
    // R7（draft_timeline_evidence §決定1）: 高卒新人だけの「期限付き未成熟負債」。
    //   実測（note/ハマノンタン 2010-2019 定義に整合: 高卒野手の「1年目50打席以上」率）:
    //   シム12.9% vs 現実5.5%＝約2.3倍甘い。aging.profiles.grow を上げる方式は初期ロスター
    //   生成にも同じ値が使われて1年目較正・多年ドリフト帯を破る（実測で確認・不採用）ため、
    //   generateRookie 限定で refAge未満の年齢ぶんの負債を積み、applyAging が毎年 repayPerYear
    //   ずつ返済して0へ収束させる（＝一時的な未成熟。大卒/社会人はrefAge以上＝負債0で据え置き）。
    youthDebtRefAge: 22,
    youthDebtPerYear: 0.8, // refAge未満 1歳ごとの初期負債(rating)。高卒(18)は最大4年分
    // 返済ペース(youthDebtRepayPerYear)は applyAging が消費するため cfg.tuning.aging 側に置く
    // 世代生成（§15 ドラフト）: 新人の年齢層。高卒18/大卒22/社会人25相当の混合分布。
    cohort: {
      hsAge: 18, // 高卒相当
      colAge: 22, // 大卒相当
      corpAge: 25, // 社会人相当
      hsShare: 0.5, // 高卒の割合
      colShare: 0.35, // 大卒の割合（残り＝社会人）
    },
    surplusPerType: 3, // ドラフトプールの余剰（各(role,pos)型で 空き数＋これ）。選択肢＝評価差の発現＝宝の源

    // R7（draft_timeline_evidence §決定2）: ドラフトを「大量に獲って淘汰する」構造にする。
    //   引退枠だけでは NPB実態（支配下約10人/球団年の入れ替わり）に届かないため、伸び悩んだ
    //   若手（cullMinAge-cullMaxAge・真の新人/確立ベテランは対象外）の当季実観測が低い順へ
    //   追加で戦力外にし、そのぶんの枠もドラフトで埋める（70人枠は不変＝淘汰が生まれる）。
    //   see: src/game/index.mjs runProspectCulling
    draft: {
      targetVacanciesPerTeam: 6, // 引退＋追加淘汰で毎年これに届くまで空ける（較正ヘッドルーム制約で6に抑制。NPB実態は約10）
      cullMinAge: 23, // これ未満（デビュー数年目）は追加淘汰の対象外＝新人保護
      cullMaxAge: 29, // これ超は通常の引退/FA/トレードに任せる
      // 新人の真値分布に右の歪みを入れる案（現行=正規分布。現実=大多数が凡庸/少数の大当たり）。
      //   bustProb の確率で mean=-skewBustMag/2 の「伸び悩み」オフセット、残りは平均 starScale
      //   （p*m/(2*(1-p))・解析的にE[skew]=0を狙う）の指数裾「大当たり」オフセットを足す設計。
      //   ★不採用（skewBustProb=0で無効化）: E[skew]=0 は clampRating(20-80) の前提であり、
      //   実際は下限20に張り付く負側が上限80に張り付く正側より多く裾を失う（非対称クリップ）ため
      //   実測で系統的な負ドリフトが生じ、多年ERA帯[3.3,4.6]を破った（test/game_multiyear で検出）。
      //   クリップを考慮した再設計をしない限り復活させないこと。
      skewBustProb: 0,
      skewBustMag: 3.0,
    },

    // 球団AI評価関数の球団差（§13/§15）: 各球団が生成時に固定で引く「評価の癖」。
    //   wDef<1 の球団が多数＝守備/位置価値の系統的な過小評価（市場の非効率）。
    //   稀に wDef>1 の「守備を正しく重める球団」が混じり、他球団の捨てた宝を拾う（守備版マネーボール）。
    profile: {
      wBatMean: 1.0, wBatSd: 0.12, wBatMin: 0.5, wBatMax: 1.6, // 打撃/投球コアの重み（球団差小）
      wEyeMean: 1.0, wEyeSd: 0.45, wEyeMin: 0.2, wEyeMax: 2.2, // 出塁(選球眼)重視度（球団差大）
      wDefMean: 0.62, wDefSd: 0.42, wDefMin: 0.05, wDefMax: 1.8, // 守備/位置価値の重み（多くが<1＝過小評価）
      ageBiasMean: 0.5, ageBiasSd: 0.35, ageBiasMin: 0, ageBiasMax: 1.6, // 年齢バイアス（若手志向のペナルティ/歳）
      noiseSdMean: 6, noiseSdSd: 2, noiseSdMin: 2, // スカウト観測ノイズSD（rating単位・球団ごとの評価の荒さ）
      // R7（決定5）: 救援の過大評価（R²=0.051 が先発0.315/野手0.399に対し桁違いに小さいのに
      //   市場は高く買う）。多くの球団が過大評価(>1)し、稀に正しく評価(≈1)する球団が
      //   救援に金をかけず安く勝つ（レイズ型）の土台になる。
      wRelieverMean: 1.3, wRelieverSd: 0.35, wRelieverMin: 0.4, wRelieverMax: 2.4,
      // Wave D（gm_analytics_spec.md）: 球団の「セイバー理解度」(0..1)。teamEvalProfile が
      //   独立シード hashSeed(masterSeed,'evalprofile',teamId,'saber') で引く（既存 r の連番draw
      //   には絶対に足さない＝足すと全球団の評価プロファイルが世界ごと引き直しになるため）。
      //   高いほどトレード時の主観価値に regressedValueOf（回帰調整済み評価）を強く反映する。
      saberSavvyMean: 0.5, saberSavvySd: 0.25, saberSavvyMin: 0, saberSavvyMax: 1,
    },
    // 評価成分のスケール（守備/出塁/位置価値の rating 換算）。
    eval: {
      eyeScale: 1.0, // 出塁(eye)成分のスケール
      posScale: 1.4, // 位置価値(posAdj)→rating換算スケール（守備マインド球団だけが重める）
      armW: 0.5, // 守備成分への肩(arm)寄与
      laW: 0.5, // 打撃成分へのLA寄与
      // R7（決定5）: 救援シェイプ（低スタミナ＝救援適性）ほど「stuff/velo」が球団の目に眩しく映る
      //   （実際の勝利貢献はスタミナに比例して薄いのに、市場は電光石火の球速/球威に過大な値を付ける）。
      relieverShapeStamina: 35, // これ以下のスタミナを「救援シェイプ」とみなす（0-1に正規化する下端）
      relieverShapeFull: 50, // これ以上のスタミナは「先発シェイプ」＝救援過大評価バイアスは0
      relieverOvervalueW: 0.5, // 救援シェイプ度 × (stuff/velo超過分) → 過大評価点への換算
    },

    // H2（phaseH_fun_spec）: プレイヤー参加型ドラフトの「スカウトレポート」表示ノブ。
    //   真値は絶対に見せない＝すべて obsTool（真値+球団固有ノイズ）／evaluateProspect（球団の
    //   癖込みの評価）／公開情報（年齢）だけから作る（trueAbility を直接は参照しない）。
    scoutReport: {
      // 総合等級 S/A/B/C/D: 自球団評価(evaluateProspect)の、現在のプール内での分位点で相対化。
      gradeBreaks: { S: 0.9, A: 0.7, B: 0.4, C: 0.15 }, // 分位点(0-1)がこれ以上でその等級（以下はD）
      // ツール別5段階（1-5）: obsTool値（20-80相当）をこの境界で離散化。
      toolBreaks: [35, 45, 55, 65],
      // 伸びしろ見立て: 「典型的なピーク年齢」（config固定値・個体の真のpeakAgeは参照しない）と
      //   公開情報の年齢との差＋スカウトノイズで3段階（大器/並/完成品）に分類する。
      referencePeakAge: 27,
      upsideNoiseSd: 1.5, // 年齢差に乗せるスカウト評価のブレ（年相当）
      upsideBigThreshold: 5, // (referencePeakAge-age)+noise がこれ以上で「大器」
      upsideDoneThreshold: 0, // これ以下で「完成品」（残りは「並」）
      // 世代内評判: 全球団平均評価（evaluateProspect の12球団平均）のプール内分位点。
      hypeTopPct: 0.85, // これ以上で「目玉」
      hiddenGemGapPct: 0.3, // (自球団分位−全球団平均分位)がこれ以上（かつ目玉でない）で「隠し玉」
      previewCount: 5, // ドラフト前ニュース「今年の目玉」で報道する人数
    },

    // 育成/支配下 二層（§12.1）: 育成枠＝観測ノイズ大＆下振れで安く獲れる箱。
    //   昇格判定＝育成の観測成績が閾値超で支配下登録（＝這い上がり）。「稀に」起きるよう閾値を高く。
    farm: {
      perTeamSignsPerYear: 2, // 毎オフに各球団が獲る育成選手数（ドラフト漏れ＝過小評価された surplus から）
      perTeamMax: 45, // 育成枠の上限（超過は観測下位を解雇）。F2-1: 初期生成の育成10-40人を収容できる幅へ拡大
      maxAge: 26, // これを超えた育成選手は解雇（大成せず箱を空ける）
      promoteThreshold: 57, // 育成の「観測成績」がこれ超で支配下登録（稀）
      promoteObsNoiseSd: 6, // 昇格判定の観測ノイズSD（球場が薄い＝観測が荒い）
      promoteObsBias: -2, // 観測は下振れ方向にバイアス（育成は観測が歪む・§12.2）
      // F2-3: 昇格判定を二軍実成績ベースへ強化（§12.1）。当年の二軍statlineが「観測」へ加点される
      //   （二軍で打った/抑えた育成ほど昇格しやすい。標本が薄いと信頼度加重で効きが弱まる）。
      promotePerfTrustPA: 100, // 二軍打撃観測の信頼度半飽和PA
      promotePerfTrustOuts: 120, // 二軍投球観測の信頼度半飽和アウト数（40回）
      // ★R4: 育成の昇格査定に二軍の守備(UZR)・走塁(BsR)の観測を入れる（旧実装は打撃/防御率のみ）。
      promoteDefW: 0.9, // 二軍の観測守備＋走塁 1得点あたりの昇格評価点
      promoteDefTrustInnings: 200, // 守備観測の信頼度半飽和イニング
      promoteWobaW: 60, // 二軍観測wOBA偏差 → 昇格観測点の換算（野手）
      promoteRa9W: 1.5, // 二軍観測RA9偏差 → 昇格観測点の換算（投手）
      promoteRa9Ref: 4.5, // 二軍観測RA9の基準（これより良ければ加点）
    },

    // ------------------------------------------------------------------------
    // C3b 選手市場: FA・トレード・戦力外/拾い上げ（§15 / §12.2）。
    //   src/game/transactions.mjs が消費。すべてオフシーズン遷移（2年目以降）でのみ発火し、
    //   1年目レギュラーシーズン（既存50較正）には一切効かない。移動はすべて同(role,primaryPos)の
    //   1:1 スワップ／循環＝球団あたりの役割・守備位置構成を厳密に不変に保つ（人口/構成恒常）。
    //   三層構造: 入札・受諾・拾い上げの査定は evaluateProspect（観測ツール＋スカウトノイズ×球団の癖）。
    //   放出（戦力外）判定だけは "実際の観測成績"（当該シーズンの生 statline）で下す＝出場機会に
    //   依存して歪む（少PA→ショボく見える＝上林型、不振→板山型）。査定を違える他球団が拾って生き返る。
    // ------------------------------------------------------------------------
    // R7（決定3）: 球団の「優勝の窓」— 前年までの成績だけから毎年純関数で導出する（新規の永続
    //   状態は持たない＝save/loadに一切手を入れない）。SABR BRJ 2018（Jordan）の実測に整合:
    //   窓の平均長6.14年・中央値5年、閉鎖判定は「2年連続で非contention」の時間的ヒステリシス。
    //   src/game/market.mjs の teamWindowState(teamId, teamHistory, cfg) が消費。
    window: {
      contendWinPct: 0.5, // これ以上の勝率(前年)を「contending」とみなす下限
      lookbackYears: 2, // 判定に使う直近年数（この年数ぶんteamHistoryを遡る）
      draftBonus: 6, // contending→大社(即戦力)/rebuilding→高卒(素材) への指名時ボーナス(rating)
    },
    fa: {
      minAge: 30, // 国内FA権発生の下限年齢（年数条件の簡略代理・§15）
      maxAge: 36, // これ超は市場価値が薄く宣言しない（引退圏）
      declareRate: 0.06, // 資格者が宣言する確率（決定論rng・毎オフ少数）
      bidMargin: 8, // 移籍成立に必要な「最高入札−現球団評価」の下駄（評価差が要る＝分かれる）
      protectCount: 28, // 人的補償のプロテクト人数（非プロテクト＝残り5から補償を出す）
      longContractAge: 34, // これ以降の長期契約はリスク（契約年数フレーバー・§15）
      maxYears: 4, // 契約年数の上限（フレーバー表示用）
      // H5-A（phaseH_fun_spec）: FA入札の実弾化。「提示salary」= finance.mjs salaryFromValue
      //   （当該選手の当季観測貢献量から算出）が本閾値を超えないと"本気の入札"とみなさない
      //   （下限未満＝格安の一言二言で片付く選手＝予算を割く価値なし）。
      salaryFloor: 250,
    },
    trade: {
      margin: 6, // 双方winに必要な各球団の純利得の下駄（churn抑制）
      maxPerYear: 6, // AI-AI 成立トレードの年間上限
      protectCount: 28, // 放出プールのプロテクト（非プロテクト同型のみ交換可）
      // H5-A: 同型1:1スワップに「年俸差」の許容上限を追加（実弾化）。双方winの評価差だけでなく
      //   現行契約(salaryOf)の差もこの範囲内でないと成立しない＝高年俸選手を安い選手で釣る
      //   一方的な財布勝負を防ぐ（AI/プレイヤー対称）。p75相当（実測: 中央値300・p75≈877）に設定。
      salaryDiffMax: 1000,
      // R7（決定4）: 「今⇄将来」の意図的な非効率（Dayn Perry/Neil Painesの実証:
      //   デッドライン補強の実際の貢献は総VORPの2.2%・WS制覇との相関はほぼ無/マイナスなのに
      //   contending球団は上位プロスペクトを過大な代償で払う）。
      //   contending は「即戦力(veteranAge以上)」を受け取る時だけ評価にプレミアムを乗せて
      //   過大評価（＝将来を安く手放す）、rebuilding は「若手(youthAge以下)」を受け取る時に
      //   同様のプレミアムを乗せる（＝即戦力を安く手放す）。twin-winの margin 判定にだけ効く。
      veteranAge: 30, // これ以上を「即戦力」とみなす（contendingの買い対象）
      youthAge: 24, // これ以下を「将来」とみなす（rebuildingの買い対象）
      windowPremium: 10, // 窓に合致する獲得選手の自己評価への上乗せ（買い手の過大評価分）
    },
    release: {
      replacementWoba: 0.29, // 代替水準（観測貢献量 =（観測wOBA−これ）×PA）＝拾い上げ後の "観測改善" の物差し
      replacementFip: 4.6, // 投手の代替FIP（観測貢献量 =（これ−観測FIP）×IP/9）
      // 戦力外スコア = 観測貢献量 − 出場機会ペナルティ（少PA/少IP＝未確立でショボく見える＝上林型／
      //   高PAの不振＝板山型 も負に沈む）。放出はこのスコアが threshold 未満の非若手から worst 順に。
      fullPA: 300, // これ未満のPAは「出場機会が薄い」＝ペナルティ対象（規定打席の目安）
      ptPenaltyBat: 0.03, // 不足PA 1 あたりの戦力外スコア減（少出場ほど切られやすい＝渋滞の犠牲）
      fullIP: 35, // これ未満のIPはペナルティ対象（先発/中継ぎ両にかかる緩い目安）
      ptPenaltyPit: 0.18, // 不足IP 1 あたりの戦力外スコア減
      threshold: -3.0, // 戦力外スコアがこれ未満で戦力外候補
      minAge: 26, // これ未満は戦力外にしない（若手は観測が薄くても切らず育てる）
      maxCutsPerTeam: 2, // 各球団が毎オフ出す戦力外候補の上限（worst score から）
    },
    // ------------------------------------------------------------------------
    // Wave D（gm_analytics_spec.md §Wave D）: トレードAI受諾のセイバー視点。
    //   src/game/transactions.mjs regressedValueOf/subjectiveTradeValue が消費。
    //   saberSavvy 自体の分布は tuning.market.profile（既存プロファイルノブと同じ置き場・上記）。
    // ------------------------------------------------------------------------
    saber: {
      // 野手: BABIP乖離の平均回帰補正（定説: インプレー打球の結果=BABIPはリーグ平均へ回帰する）。
      //   当季BABIPがリーグ平均±babipDevThresholdを超えて乖離していれば、乖離の
      //   babipRegressFactor（=半分）ぶんをリーグ平均側へ戻す（超過分だけを戻す訳ではなく、
      //   乖離全体の半分を戻す＝定説の「回帰」を素直に表現する簡易実装）。
      babipDevThreshold: 0.030,
      babipRegressFactor: 0.5,
      leagueBabip: 0.308, // 回帰の基準（calibrate babip目標帯[.298,.318]の中央値）
      // 野手: 少PA縮約（regression to the mean・簡易ベイズ）。PA/(PA+paRegressConstant) で
      //   リーグ平均wOBA(tuning.mgr.wobaPrior)へ縮約する。mgr.wobaPriorPA(=60)は「観戦AIの
      //   その場の采配判断用」の弱い縮約であり、市場評価用にはより強い縮約定数を新設する。
      paRegressConstant: 300,
      // 投手: FIP(DIPS準拠)を維持しつつ、K-BB%由来の推定ERA（kwERA式=既存 tuning.metrics.kwERA
      //   を流用）を fipWeight:kbbWeight（既定7:3）で合成する（K-BB%はFIPよりHR/FB変動に強い
      //   先行指標という定説＝「K-BB%が良いのにFIPが悪い」場合を将来重視で補正する）。
      fipWeight: 0.7,
      kbbWeight: 0.3,
      leagueFip: 3.8, // 少IP縮約の基準（calibrate fip目標帯[3.6,4.0]の中央値）
      ipRegressConstant: 70, // 少IP縮約の定数（IP/(IP+これ)でleagueFipへ縮約）
      // 年齢割引（老化の定説・ageBiasとは独立の客観項）: 30歳超は1歳ごとに残存価値を逓減する。
      ageDiscountStartAge: 30,
      ageDiscountPerYear: 0.04, // 1歳あたりの逓減率
      ageDiscountFloor: 0.5, // 逓減の下限（0に潰さない）
      // トレードAIの主観価値合成（transactions.mjs subjectiveTradeValue）:
      //   regressedValueOf/observedValueOf は runs 単位、evaluateProspect は rating 単位で
      //   スケールが違う（直接の線形加重blendはスケール崩壊する）ため、両者の差分
      //   (regressed−naive)を runToRatingScale で rating 相当へ変換し、savvy に応じて
      //   評価の「従来評価（rating）」へ加算する（savvy=0で従来評価と完全一致・savvy=1で
      //   フル反映。(1−savvy)×従来+savvy×regressed の趣旨をスケール整合させた実装）。
      runToRatingScale: 0.5,
      savvyDeltaCap: 15, // 上の加算量の上限（rating点・暴走防止）
      // ポジション需要項: 受け手の弱点位置(gmBoard.positionStrengthMap の weak)を埋める選手は
      //   主観価値を(1+posNeedBonus)倍、飽和位置(saturated)の選手は(1−posSurplusPenalty)倍。
      posNeedBonus: 0.12,
      posSurplusPenalty: 0.12,
    },
  },

  // ==========================================================================
  // H5-A: 経営レイヤー第1段階「年俸予算」（phaseH_fun_spec H5・fun_design_evidence §4柱5）。
  //   team.finance = {budget, payroll} を新設し、既存 transactions.mjs の salary（旧・純フレーバー）
  //   を実弾化する: FA入札は「提示salaryが下限超かつ予算内」が成立条件に追加、トレードは
  //   salary差が許容内（market.trade.salaryDiffMax）、契約更改で予算超過の非プロテクト高給選手は
  //   戦力外候補ルートへ合流する。src/game/finance.mjs（salaryOf/salaryFromValue/budget生成）＋
  //   src/generate.mjs teamFinanceProfile（budgetの決定論付与・§13 teamEvalProfile と同じ流儀）。
  //   すべてオフシーズン限定（1年目レギュラーシーズンは非干渉）。AI/プレイヤー対称の制約
  //   （どちらの入札/起案/更改にも同じ判定が掛かる）。
  //
  //   ★設計方針（実装時の判断メモ）: budget は「生成時に球団プロファイルから決定論付与」
  //   （teamEvalProfile と同じ独立シード 'finance'）し、H5-Cでファン人気連動の年次見直しが入るまでは
  //   固定帯（キャリア中不変）とする。旧セーブは finance フィールドが無い＝load時に同式で
  //   決定論補完できる（personality/H3-1 と同じ「後付け可能」構造）。band は「実測: 財力差を無視した
  //   場合の球団平均総年俸が約40000〜60000（万円相当・観測貢献量ベースのsalaryFromValue合計、
  //   支配下70人）」という実測（H5-A実装時の3年3seedプローブ）を踏まえ、mean をその中心よりやや高めに
  //   置いて「平均的には予算内、下位球団だけ本当に苦しい」形にした（financeが常時ボトルネックだと
  //   FA/トレード成立数が激減し realism WATCH が壊れるため）。
  // ==========================================================================
  economy: {
    defaultSalary: 300, // p.contract 未設定（更改未経験）時のフォールバック年俸＝salaryBase(v=0)と同値
    salaryBase: 300, // 契約更改salaryの基準額（旧runContractRenewalの300を式として独立させたもの）
    salaryPerValue: 40, // 観測貢献量(v) 1 あたりの年俸換算（旧runContractRenewalの40と同値＝実弾化で分布は変えない）
    salaryFloor: 50, // 契約更改の最低年俸（旧runContractRenewalの50と同値）
    protectCount: 28, // 予算超過球団の戦力外候補選定で使うプロテクト人数（market.fa/tradeと同枠）
    maxBudgetCutsPerTeam: 2, // 予算超過球団が1オフに戦力外候補ルートへ送る上限人数
    // H5-C: ファン関心と収入の閉ループ（OOTP実挙動パターン・fun_design_evidence §1.3）。
    //   fanInterest(0-1)は毎オフ「勝率分位への回帰＋イベント修正値」で更新され、budget を
    //   市場規模×(floorMult+spanMult×fanInterest) で年次見直しする（=成績→ファン→予算の閉ループ。
    //   係数帯 0.75-1.25 と budget帯clampの二重で有界＝金満/貧乏の暴走を構造的に防ぐ）。
    fan: {
      init: 0.5, // 初期値・旧セーブ補完値（係数1.0＝H5-A固定帯と同値）
      regress: 0.25, // 勝率分位への年次回帰率（OOTPの「緩やかに成績を追随」）
      championBonus: 0.12, // リーグ優勝（勝率1位）の一過性ブースト
      starSalary: 3000, // これ以上の年俸の選手を「スター」とみなす（FA流出でファンが怒る閾値）
      starLossHit: 0.06, // スター1人のFA流出あたりの fanInterest 低下
      min: 0.05, // 下限（ファンがゼロにはならない）
      max: 1,
      // ★budget連動は既定OFF（floor=1.0/span=0＝budgetは市場規模のまま静的）。ONにすると市場の
      //   結果が毎年組み変わり、多年ERA帯(test/game_multiyear)が軌道の混沌増幅で壊れる（H4のAI方針・
      //   R7と同じ失敗様式を実測）。ui.mjs の uiConfig() が実プレイでのみ 0.75/0.5 を上書きする
      //   （interactiveDraft/allowFiring と同じ「headless既定OFF・UIのみON」パターン）。
      //   fanInterest の追跡・表示・オーナー目標への入力は既定でも生きている（表示は非干渉）。
      budgetFloorMult: 1.0, // budget係数の下端（既定=静的。UIプレイでは0.75）
      budgetSpanMult: 0, // budget係数の幅（既定=0。UIプレイでは0.5＝fanInterest=1で1.25倍）
    },
    budget: {
      // 財力差（球団プロファイル・§13と同じ発想）: masterSeed×teamIdの独立シードから決定論的に引く。
      // ★H5-A実装時の実測較正: 当初 mean=50000（実測payroll平均40000-60000のやや上）で試したところ
      //   球団・年の48.8%が予算超過＝FA成立数が9.5→5.7（-40%）へ落ち込んだ（5seed×8年プローブ）。
      //   spec のゲート「市場成立数が激減しないこと」に抵触するため、平均payroll分布の実測上限
      //   （約60000）を budget の中心に置き直し、予算超過が「一部の貧乏球団だけが時々踏む」
      //   程度（球団・年の1-2割目安）になるよう再較正した。
      mean: 68000, // 中心値（万円相当の総額）
      sd: 17000, // 球団間の財力差の標準偏差
      min: 36000, // 下限（貧乏球団でも市場から完全排除されない下支え）
      max: 110000, // 上限（金満球団の頭打ち）
    },
  },

  // ==========================================================================
  // H5-B: オーナー目標・信任・解任（phaseH_fun_spec H5-B）。src/game/owner.mjs が消費。
  //   プレイヤー球団のみ・yearIndex>=1 のみ（1年目非干渉）。効果は表示＋解任時の playerTeamId
  //   切替だけ＝エンジン非干渉。目標は teamWindowState と整合（rebuilding に優勝を要求しない）。
  // ==========================================================================
  ownerGoals: {
    maxGoals: 2, // 1年に提示される目標の最大数（先頭=high優先度・以降=low）
    trustStart: 50, // 信任の初期値（0-100）
    fireBelow: 20, // 信任がこれ未満でオフに解任イベント（cfg.game.allowFiring=true時のみ）
    pleaBonus: 10, // 留任嘆願（キャリア1回限り）の信任回復量
    transferResetTrust: 40, // 移籍受諾後の新球団での信任初期値
    high: { success: 12, fail: -15 }, // high優先度目標の達成/失敗の信任増減
    low: { success: 6, fail: -8 }, // low優先度
    aClassDelta: 3, // リーグ3位以内の付帯加点
    bClassDelta: -3, // Bクラスの付帯減点
    winPctMin: 0.48, // 「勝率X以上」目標の閾値（contending/neutral）
    rebuildWinPctMin: 0.4, // rebuilding期の控えめな勝率目標
    youthAgeMax: 25, // 「若手」の定義（この年齢以下）
    youthPAMin: 800, // 若手起用目標の必要合計打席
  },

  // ==========================================================================
  // 演出・記録（C4・§16/§17）: 表彰・ニュース・記録・二つ名の判定定数。
  //   src/game/awards.mjs / src/game/news.mjs が消費。すべて "観測成績/WAR" ベースで
  //   選定・命名する（trueAbility を直接見ない＝三層構造）。集計値のみを参照し、
  //   1年目レギュラーシーズン（既存50較正）には一切干渉しない（表示・集計のみ）。
  // ==========================================================================
  awards: {
    // 二つ名（§16 Lv4回避=画像なし）: 通算 "観測" 集計のパターンで自動付与（priority順に判定）。
    //   trueAbility 非参照＝出場して積み上げた観測（H/HR/SB/BB%・IP/BB9/K9/S）だけで決める。
    nickname: {
      ipGate: 400, // 投手二つ名を付ける最低通算IP（未満は「未知数」）
      paGate: 1500, // 野手二つ名を付ける最低通算PA
      precisionBbPer9: 2.2, // 精密機械（制球特化＝通算BB/9がこれ以下）
      strikeoutKPer9: 9.0, // ドクターK（奪三振＝通算K/9がこれ以上）
      closerSv: 150, // 守護神（通算セーブ）
      workhorseIp: 1800, // 鉄腕（通算IP）
      sluggerHr: 200, // 大砲（通算本塁打）
      bigSluggerHr: 350, // 巨砲（通算本塁打・大砲より上位）
      speedSb: 200, // 韋駄天（通算盗塁）
      hitMachineH: 1500, // 安打製造機（通算安打）
      hitMachineAvg: 0.3, // 安打製造機の通算打率ゲート
      onbaseBbPct: 0.135, // 出塁の職人（通算BB%）
      isoSlugger: 0.2, // スラッガー（通算ISO）＋中量HR
      isoSluggerHr: 120,
    },
    // 通算マイルストーン（架空リーグ記録文脈・通算 careerStats から crossing 検出）。
    milestones: {
      hits: [1000, 1500, 2000],
      homeRuns: [200, 300, 400],
      wins: [100, 150, 200],
      saves: [100, 150, 250],
      strikeouts: [1000, 1500, 2000],
    },
    topN: 10, // リーグ記録トップN（シーズン/通算）
    gloveMinInnings: 400, // 守備の栄誉賞の最低守備イニング（過小サンプル除外）
    rotyMinPa: 100, // 新人王の最低打席（scrub 除外）
    rotyMinIp: 20, // 新人王の最低投球回

    // ========================================================================
    // H3-2: 評判ラベル「メディア評」（phaseH_fun_spec H3・fun_design_evidence §4柱4）。
    //   通算 "観測" 集計と state.injuryLog（試合中の離脱イベント＝観測可能な公開情報）だけから
    //   複数タグを独立判定する（nicknameFor と同じ三層構造の作法＝trueAbility は一切見ない）。
    //
    //   ★設計変更メモ（実装時の実測で判明）: spec 原案は Clutch/WPA/aLI（文脈指標）を主軸に
    //   想定していたが、文脈指標(RE24/WPA/LI)は season.mjs の opts.context（2パス導出）でのみ
    //   埋まり、実際のキャリアモード（game/season_runtime.mjs の日次進行）はこのフラグを立てて
    //   いない＝プレイ中は常に0（calibrate.mjs 等の単発フル計算専用ツールでしか埋まらない）。
    //   Clutch/WPA/gmLI 基準ではラベルが実プレイで一切発火しない死んだ機能になるため、
    //   実際に埋まる観測値へ土台を差し替えた:
    //     - 勝負師/劇場型/ブレーキ（野手）: RISP splits（対RISP成績。§B3b・game.mjs が毎打席
    //       無条件で計上＝文脈指標と違い常時有効）＋併殺率(gdp/gdpOpp)。ただし splits は
    //       compactCareerStats が直近 keepDetailYears 年より古いシーズンから削るため、
    //       「保持されている年度分」だけの集計になる（overall/RISPを同じ年度集合でフェアに
    //       比較する＝careerRispEdge）。「メディアは直近の記憶に残る活躍で物語を作る」という
    //       趣旨には却って合致する。
    //     - 火消し/敗戦処理（投手・救援専業）: 登板時レバレッジ(gmLI)の代わりに
    //       セーブ+ホールド率(sv+hld)/救援登板数（既存のSV/HLD計上はcontext非依存で常時有効）。
    //   閾値は12seed運用（burn-in15年+6年）の実測分布から「上位/下位 概ね1-2割が該当」する
    //   水準に較正（tools/calibrate.mjs 相当の帯較正ではなく、この機能専用の実測サンプリング）。
    // ========================================================================
    reputation: {
      minRispAb: 150, // RISP集計のサンプルゲート（保持年度分のRISP対象打席の元になる通算AB）
      clutchHot: 0.09, // 勝負師: RISP OPS − 通算OPS（同一年度集合）がこれ以上
      clutchCold: -0.04, // ブレーキの補助条件: 同エッジがこれ以下
      dramaRispShare: 0.31, // 劇場型: 全打席に占めるRISP打席の割合がこれ以上（平均的には0.25-0.29程度）
      gdpRateBrake: 0.42, // ブレーキ: 併殺率(gdp/gdpOpp)がこれ以上
      minGdpOpp: 60, // ブレーキの併殺機会ゲート（過小サンプル除外）
      injuryGlassGames: 150, // ガラスの体: 通算離脱試合数(gamesLost合計)がこれ以上
      injuryGlassCount: 4, // または通算故障件数がこれ以上
      glassMinSeasons: 2, // ガラスの体の最低通算シーズン数（デビュー直後の一度きりの負傷を除外）
      ironManSeasons: 8, // 鉄人: 通算出場シーズン数がこれ以上
      ironManMaxInjuries: 1, // かつ通算故障件数がこれ以下
      reliefMinG: 100, // 火消し/敗戦処理の通算救援登板数(g-gs)ゲート
      firemanSvHldRate: 0.40, // 火消し: 救援登板に占める(セーブ+ホールド)の割合がこれ以上
      mopupSvHldRate: 0.05, // 敗戦処理: 同割合がこれ以下（高レバレッジ場面をほぼ任されない）
    },
  },

  // ==========================================================================
  // H1: ストーリーライン（連続ニュース・ライバル・引退ロード・phaseH_fun_spec H1）。
  //   src/game/storylines.mjs が消費。表示層のみ（エンジン非干渉）。すべて観測集計/
  //   transactionLog からの純関数（真値を一切参照しない＝三層構造の堅持）。
  // ==========================================================================
  storylines: {
    // タイトル争い（H1-1）: 首位と2位の差がこれ以内なら「激戦」フラグ。rate=打率/防御率など率系、
    //   count=HR/打点/盗塁/勝利/セーブ/奪三振など数え上げ系（カテゴリ別に単位が違うため二本立て）。
    raceCloseMargin: { rate: 0.006, count: 3 },
    rookieRaceTopN: 5, // 新人王レースの表示人数（リーグ別）
    recordPaceThreshold: 1.05, // シーズン記録の105%超ペースで「記録ペース」ニュース
    recordPaceMinProgress: 0.5, // 消化50%未満はペース判定の対象外（序盤のノイズ除外）
    digestMaxItems: 6, // 「今週の見どころ」節に載せる最大件数
    draftClassMax: 6, // P5「今年の逸材」ドラフトクラス見出しの最大件数（draftClassHeadlines）
    retirementRoad: {
      ageThreshold: 37, // これ以上＋通算マイルストーン持ちを「引退ロード候補」ニュースの対象に
      batterMilestones: { hits: 500, homeRuns: 100 }, // いずれか到達で候補（awards.milestonesより低い早期到達バー）
      pitcherMilestones: { wins: 50, saves: 50, strikeouts: 500 },
      ceremonyMinPA: 2000, // 「引退セレモニー」節を出す通算打席の下限（野手）
      ceremonyMinIP: 600, // 同・通算投球回の下限（投手）
      ceremonyMinAwards: 1, // 通算受賞件数がこれ以上ならPA/IPが薄くても功労者扱い
    },
    // Q3（thyroxin/research…20260723 Q3・「記憶に残る一日」特別デー）: game/storylines.mjs の
    //   specialDaysOf が消費。低頻度（月1-2回程度）を狙った閾値＝表示層のみ・エンジン非干渉。
    specialDays: {
      // 節目リーチ: 現在の通算値からこの本数/勝数以内で次のマイルストーン（awards.milestones）に
      //   届く選手がいれば、自チームの次の未消化試合を「節目リーチ」の特別デーにする。
      milestoneReach: { hits: 5, homeRuns: 3, wins: 2, saves: 2, strikeouts: 5 },
      // P1-4（thyroxin/reviews/game_review_20260724.md）: 旧定義「相手との直接ゲーム差1.5以内」は
      //   下位同士の凡戦も拾って試合日の30.8%で発火した（均衡設計下では下位球団同士も接近しがち）。
      //   「自分または相手が首位と僅差」＝真の首位争いに限定する定義へ変更（希少化）。
      //   12seed sweep実測（2026-07-24）: 1.5×1/3では依然発火過多。0.5×0.6で月2.8回＝レビュー目標
      //   「月1-3回」帯に収束（「首位攻防はシーズン終盤の物語」という意味論にも合致）。
      pennantMaxGb: 0.5, // 首位攻防戦: 自チームまたは相手が首位とこのゲーム差以内（同リーグ）
      pennantMinProgress: 0.6, // 首位攻防戦: シーズンをこの消化率（自チーム試合数/gamesPerSeason）以降のみ判定（終盤の優勝争いに限定）
    },
    // R1+R7+R8（thyroxin/research…データストーリーテリング調査）:「アナリストコラム」
    //   （game/analystColumn.mjs が消費）。観測成績(rt.stats)のみの純関数・表示層のみ・エンジン非干渉。
    analyst: {
      maxItems: 4, // ニュースタブ「🔬 アナリストの目」節に載せる週次の最大件数
      extremeMinPool: 5, // 極端値（リーグ1位/最下位）を語るのに必要な最低母集団（規定到達者数）
      divergenceXwobaMin: 0.025, // xwOBA−wOBA 乖離の下限（打者・意外性型/出来すぎ警報の判定）
      divergenceEraSieraMin: 0.75, // ERA−SIERA 乖離の下限（投手・意外性型/出来すぎ警報の判定）
      comparisonMaxCareerYear: 3, // 「若手」とみなす最大プロ年数（比較型: 殿堂の同年目ペースとの対比）
      comparisonMinProgress: 0.3, // シーズン消化率がこれ未満はペース比較の対象外（序盤ノイズ除外）
      comparisonPaceRatio: 0.85, // 若手ペースが殿堂の同年目実績のこの比率以上なら「同水準」
      gameHighlightLookback: 5, // R7: 直近何試合まで遡って「その日の一番」を探すか
      gameHighlightMinOuts: 15, // R7好投判定: 最低アウト数（5回=15アウト）
      gameHighlightMinKbb: 5, // R7好投判定: 最低 奪三振−与四球
      gameHighlightMinHr: 2, // R7快音判定: 1試合の最低本塁打数
      wpaHiddenWrcMax: 100, // R8: 「打撃成績が地味」とみなす wRC+ の上限（リーグ平均=100未満）
      wpaHiddenMinWpa: 0.3, // R8: 隠れWPAリーダーとして拾う最低WPA
    },
    // Wave B（thyroxin/specs/gm_analytics_spec.md・GM分析仕様）: フォーム判定（好調▲/不調▼）。
    //   game/form.mjs が消費。自チーム選手のみ playerGameLog の窓（box集計）から算出する純関数。
    //   表示層のみ・エンジン非干渉・真値/能力値レーティング非参照（観測statlineのみ）。
    form: {
      batWindowGames: 10, // 野手: 窓＝直近何試合（自チームの playerGameLog 末尾から）
      batMinWindowPA: 20, // 窓の最低打席（未満はサンプル不足でtier=null。box近似のPA=ab+bb）
      wobaGapHot: 0.040, // 窓wOBA近似がシーズンwOBAをこれ以上上回れば好調候補
      wobaGapCold: 0.040, // 同・下回れば不調候補（対称の下限）
      babipHotGuard: 0.400, // 窓BABIPがこれ超なら「出来すぎ」警報（平均回帰の定説・好調時に付記）
      babipColdGuard: 0.230, // 窓BABIPがこれ未満なら「不運が過ぎる」警報（同定説・不調時に付記）
      babipMinBip: 10, // BABIP警報を付けるための最低インプレー打球数（ab-so-hr、ゼロ割/極小サンプル回避）
      pitLookbackApps: 3, // 投手: 窓＝直近何登板（自チームの playerGameLog を遡って登板を3件拾う）
      pitLookbackMaxGames: 40, // 3登板を探して遡る試合数の上限（先発ローテ間隔を跨いでも見つからない場合の安全弁）
      pitMinWindowOuts: 18, // 窓の最低アウト数（6回=18未満はサンプル不足でtier=null）
      kbbGapHot: 0.08, // 窓K-BB%がシーズンK-BB%をこれ以上上回れば好調候補（DIPS理論・投手の先行指標）
      kbbGapCold: 0.08, // 同・下回れば不調候補
      eraGapHot: 1.5, // 窓の目安防御率(失点/9)がシーズンERAよりこれ以上低ければ好調候補
      eraGapCold: 1.5, // 同・高ければ不調候補
    },
    // Wave C（thyroxin/specs/gm_analytics_spec.md・GM分析仕様）: GMボード（弱点・飽和・有望若手・
    //   トレード相手サジェスト）。game/gmBoard.mjs が消費。観測statline/farmStatsのみの純関数・
    //   表示層のみ・エンジン非干渉・真値/能力値レーティング非参照。
    //   ★2026-07-24 監査修正（3seed実測: 飽和★が120セル中87.5〜90%で指標として死んでいた）:
    //     ①レギュラー(positionOuts最多の機械選定)より控えの観測が上回る「逆転」が飽和の79%を占め、
    //       多面起用選手が控え枠に回っただけの誤検出だった → satMinPctl引上げ＋逆転を
    //       misallocated（起用のねじれ）として分離し saturated（真のサプラス）から除外。
    //     ②同一選手が複数ポジションの「控え」に多重カウント(19-24%) → 他ポジでregularの選手・
    //       最多出場ポジ以外での控え候補化を排除。
    //     ③救援がqualifiedIP（先発基準）を分母にされ「塞がれた」誤判定 → 役割別分母を導入。
    gmBoard: {
      minSeasonProgress: 0.2, // 消化率がこれ未満は弱点/飽和判定を出さない（序盤の少試合ノイズ除外）
      minPositionPopulation: 6, // 百分位化に使う母集団（球団の「レギュラー」値の数）の最低数
      weakPctlMax: 0.2, // 弱点=百分位がこれ以下（下位20%）
      satMinPctl: 0.8, // 飽和判定: 控えの百分位がこれ以上（n=12母集団の上位3位相当＝監査修正で0.6→0.8）
      satMisallocMargin: 0.55, // 監査修正a/c/d: 控え百分位がレギュラー百分位+この値を超えたら「逆転」
                                // （misallocated=起用のねじれ）とみなし saturated から除外する。
                                // ★実測ベースで調整: satMinPctl=0.8＋多重カウント排除(b)適用後、
                                //   satMinPctl相当まで上振れる控えの大半はレギュラーが中位以下の
                                //   チームに偏る（=真の逆転）ため、margin=0.1〜0.3程度では
                                //   saturatedが3〜12%まで痩せてしまう（7seed実測）。0.55で
                                //   saturated=13〜23%（監査目標帯15-25%に収束）・misallocatedは
                                //   なお10%前後残る＝両シグナルが共に生きる帯を実測で選定
      satMinPaFrac: 0.3, // 飽和判定（野手）: 控えの打席が「規定打席」のこの割合以上
      satMinIpFrac: 0.3, // 飽和判定（投手）: 控えの投球回が「規定投球回」のこの割合以上
      relieverIpGamesFrac: 0.45, // 監査修正b: 救援の役割別「規定投球回」相当 = チーム試合数×この値。
                                  // qualifiedIP(先発基準=試合数×1)を救援の飽和/塞がれ判定に使うと
                                  // フル稼働の救援でも規定の35%程度にしかならず誤判定になるため分離
      relieverEstablishedG: 25, // 監査修正b: 登板数がこれ以上の救援は「塞がれ」対象外
                                 // （既に相応の役割を得ている＝塞がれた有望株ではない）
      prospectMaxAge: 25, // 有望若手とみなす年齢上限
      prospectMinPA: 20, // 有望若手判定（野手）に必要な最低打席（一軍/二軍とも・小サンプルは語らない）
      prospectMinIP: 15, // 有望若手判定（投手）に必要な最低投球回
      prospectMinPctl: 0.6, // 「観測百分位が高い」の下限（これ未満は有望/格上げ候補と語らない）
      prospectThinPaFrac: 0.5, // 一軍在籍で「出場機会が細い」とみなす打席（規定打席のこの割合未満）
      prospectThinIpFrac: 0.5, // 同・投球回（規定投球回のこの割合未満・救援は分母がrelieverIpGamesFrac）
      prospectMaxItems: 20, // 狙い目リストの最大件数（超過時はUIに「他にも該当あり」を注記）
      tradeSuggestMax: 5, // トレードの窓サジェストの最大件数
      ownDepthMaxItems: 10, // 「自軍の答え（格上げ候補）」の最大件数
    },
  },
};

/**
 * ゲーム層（フェーズC）の定数。日次カレンダー境界・セーブスキーマ版・開幕年。
 *   day = 日程の「節」index（1日1試合）。週/月境界は node --test で決定論的に刻むための単純割り。
 *   NPB 1シーズン ≈ 143試合 ≈ 150節前後 ≈ 6か月 なので daysPerMonth:26 で概ね月次に対応する。
 */
export const GAME_DEFAULT = {
  schemaVersion: 2, // セーブJSONのスキーマ版（load時の互換判定。C2bでオフシーズン意味論拡張→v2）
  firstSeason: 2026, // キャリア1年目の年
  // R4: careerStats に「表示専用の内訳（スプリット/カウント別）」を残す直近年数。これより古い年は
  //   内訳を落として save の肥大を防ぐ（20年プレイで数十MBになる）。指標/表彰/記録/査定は
  //   トップレベルの生カウントのみを使うので影響しない。
  keepDetailYears: 2,
  // ★R5: ゲーム（UI）が世界を作るときに回す「前史」の年数。生成直後のリーグは
  //   「誰も故障したことがなく・通算記録も引退者もドラフト史も無い」冷えた世界なので、
  //   20年ぶんのドラフト→成長→故障→引退のサイクルを先に回してからプレイヤーに渡す。
  //   ※エンジンの newGame の既定は 0（＝較正/テストの土台は生成直後のリーグのまま）。
  //     UI だけがこの値を newGame に渡す（src/ui.mjs startNewGame）。
  burnInYears: 30, // 山本昌の実働29年が丸ごと収まる長さ（1世代が完全に入れ替わる）
  daysPerWeek: 7, // advanceTo('weekEnd') の週境界
  daysPerMonth: 26, // advanceTo('monthEnd') の月境界（143試合≈6か月）
  rookieAgeMin: 18, // 新人（ドラフト補充）の最小年齢（高卒相当）
  rookieAgeMax: 22, // 新人の最大年齢（大卒/社会人相当）
  // H2（phaseH_fun_spec）: プレイヤー参加型ドラフト会議。既定は false（後方互換最優先＝
  //   headless/既存テストは全自動のまま byte 不変。ui.mjs の startNewGame だけがオーバーライドで
  //   true を渡す）。true でも自チームの指名番以外は従来と同じAI自動（AI11球団のロジック不変）。
  interactiveDraft: false,
  // H5-B（phaseH_fun_spec）: オーナー信任による解任。既定は false（H2 interactiveDraft と同じ
  //   「後方互換最優先」パターン: headless=テスト/較正/realism/前史の advanceYear ループは絶対に
  //   中断しない）。ui.mjs の uiConfig() だけがオーバーライドで true を渡す＝実プレイでのみ解任が
  //   発生する。true でも解任は「移籍オファー受諾 or 留任嘆願(1回)」でハードゲームオーバーなし。
  allowFiring: false,
  // 打順の毎試合再構成（現代のラインナップ理論・§S3-2打順・cfg.tuning.lineup）。既定は false
  //   （interactiveDraft/allowFiring/ファン予算連動と同じ「headless既定OFF・UIのみON」パターン。
  //   ONにすると打順の継続最適化でリーグ攻撃がごく僅かに効率化し、多年運用のERA帯が yi12=4.63 で
  //   §11.3 の relaxed 上限4.6を超える＝headless較正の土台を動かすため、実プレイのみで有効化する）。
  //   OFF時は編成時(trueAbility)アーキタイプの打順を守備位置に固定する旧挙動＝baselineとbit同一。
  //   ui.mjs の uiConfig() だけがオーバーライドで true を渡す（＝プレイヤーの試合では現代的な打順になる）。
  dynamicLineup: false,
  // P1（p1_interactive_manager_spec）: 試合中の人間采配（介入観戦・代打/継投）。既定は false
  //   （interactiveDraft/allowFiring/dynamicLineup と同じ「headless既定OFF・UIのみON」第5例目。
  //   テスト/較正/realism/前史/AI球団は従来と byte 同一）。ui.mjs の uiConfig() だけが
  //   オーバーライドで true を渡す。true でも介入ログが空のまま完走した試合は全自動と bit 同一
  //   （介入は既存の choosePinchHitter/chooseReliever 呼び出し地点への人間差し替えフックのみ・
  //   判断ロジックの新設はしない）。
  interactiveManager: false,
  // P3（fun_theory_research_20260720 P3）: 週次目標（短期目標の階層）。既定は false
  //   （interactiveDraft/allowFiring/dynamicLineup/interactiveManager と同じ「headless既定OFF・
  //   UIのみON」第6例目。テスト/較正/realism/前史/AI球団は従来と byte 同一）。
  //   ui.mjs の uiConfig() だけがオーバーライドで true を渡す。true でも効果は表示＋シーズン末の
  //   信任へ小さく加算するだけ（エンジン/tuning非干渉。生成・判定は game/goals.mjs）。
  weeklyGoals: false,
  // P3: シーズン末の信任評価へ加算する週次目標達成率ボーナスの最大値（達成率100%で+この値）。
  //   tuning.ownerGoals（high:+12/low:+6等）より十分小さく抑える＝週次はあくまで補助的な効果。
  weeklyGoalTrustBonusMax: 5,
  // Q1（thyroxin/research…20260723 Q1・信頼度）: 起用の安定→年次ドリフトSD縮小の双方向ループ。
  //   既定は false（interactiveDraft/allowFiring/dynamicLineup/interactiveManager/weeklyGoals と
  //   同じ「headless既定OFF・UIのみON」第7例目。テスト/較正/realism/前史/AI球団は従来と byte 同一）。
  //   ui.mjs の uiConfig() だけが true を渡す。真値trueAbility非参照＝前季の観測statlineのみを
  //   aging.mjs の applyAging に渡して使う（三層構造の層2から層1へのフィードバック）。
  usageTrust: false,
  // Q1: usageTrust=true 時、aging.mjs の drift SD 倍率 = 1.0 − usageStabilityOf(...) × この値。
  //   streakyDriftMult(=1.25 相当の分散押し上げ)と同程度の桁に抑える小定数（tuningには置かない
  //   ＝Q1指示どおり cfg.tuning 非汚染。安定度1.0で倍率は 1.0−0.15=0.85、安定度0で1.0＝無効果）。
  usageTrustDriftSpan: 0.15,
  // Q9（thyroxin/research…20260723 Q9・介入観戦「山場だけ」モード）: 観戦開始オーバーレイの
  //   「⚡山場のみ介入」ボタンが opts.managerIntervention.minLI に渡す既定値。UIからのみ使用＝
  //   cfg.tuning非汚染（較正非干渉）。sim/manager.mjs の leverageProxy（既存の継投レバレッジ代理）
  //   をそのまま再利用した局面LIと比較する（pen.highLevThreshold=1.6と同程度の帯）。
  clutchModeMinLI: 1.5,
};

/**
 * シムのラン環境から 1-6（リーグ定数導出パス）で埋める定数の器。
 * §18の 13.3/1.216 等は「初期値」であって、ここに導出値を上書きしてから指標を確定する（2パス）。
 */
export function createLeagueConstants() {
  return {
    linearWeights: null, // {bb,hbp,b1,b2,b3,hr, outValue,...}
    wobaScale: null,
    lgwOBA: null,
    lgFIP: null,
    fipConstant: null,
    rpw: null, // Runs Per Win
    posAdjPer1350: null, // 143試合へ再スケール後の位置補正（§9・M3）
  };
}

/** 設定一式のディープコピーを配る（較正ランごとに独立させる） */
export function createConfig(overrides = {}) {
  const cfg = {
    version: CONFIG_VERSION,
    league: clone(LEAGUE_DEFAULT),
    targets: clone(CALIBRATION_TARGETS),
    tuning: clone(TUNING_DEFAULT),
    game: clone(GAME_DEFAULT),
    leagueConstants: createLeagueConstants(),
  };
  return deepAssign(cfg, overrides);
}

// --- 試合数依存の媒介変数（M3: 162試合MLB定数を143へ再スケール） -------------
/** 規定打席（≒ games×3.1） */
export function qualifiedPA(games = LEAGUE_DEFAULT.gamesPerSeason) {
  return Math.round(games * 3.1);
}
/** 規定投球回（= games×1） */
export function qualifiedIP(games = LEAGUE_DEFAULT.gamesPerSeason) {
  return games;
}
/** フル出場の守備イニング（= games×9）。posAdj の分母（旧1350の置換） */
export function fieldingInningsFull(games = LEAGUE_DEFAULT.gamesPerSeason) {
  return games * 9;
}

/** 値が [min,max] に収まるか（較正の合否判定ヘルパー） */
export function inRange(v, range) {
  return v >= range[0] && v <= range[1];
}

// --- 内部ヘルパー -----------------------------------------------------------
function clone(o) {
  return JSON.parse(JSON.stringify(o));
}
function deepAssign(base, over) {
  for (const k of Object.keys(over)) {
    const ov = over[k];
    if (base[k] && ov && typeof base[k] === 'object' && typeof ov === 'object' && !Array.isArray(ov)) {
      deepAssign(base[k], ov);
    } else if (ov !== null && typeof ov === 'object') {
      base[k] = clone(ov); // 葉のオブジェクト/配列は複製し、override元との状態共有を断つ
    } else {
      base[k] = ov; // null・プリミティブは葉として代入
    }
  }
  return base;
}
